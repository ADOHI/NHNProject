# 17. 던전 절차 생성

> **상태: 설계 확정 / 수치 잠정.**
> 던전은 **모두 절차적으로 생성**한다. 수제작 던전은 두지 않는다.
> 공간 규칙(고도 · 방 종류)은 [`07-level-design.md`](07-level-design.md) §7.2.6~7.2.9 참고.

---

## 17.1 생성기의 진짜 요구사항

**"재미있는 판"이 아니라 "추리가 성립하는 판"을 만드는 것이다.**

이 게임에서 던전은 이동 경로가 아니라 추리판이다([`07`](07-level-design.md) §7.2).
따라서 생성기의 난이도는 만드는 쪽이 아니라 **못 쓸 판이 나오지 않게 하는 쪽**에 있다.

| 실패한 판 | 왜 못 쓰나 |
| --- | --- |
| 탈출구에 도달할 수 없다 | 시작하자마자 진 판이다 |
| 숫자만으로 정답이 나온다 | 추리가 아니라 계산이 된다 |
| 단서가 전혀 없다 | 추리가 아니라 도박이 된다 |
| 입구 옆이 즉사급 | 판단할 기회 자체가 없다 |
| **선이 서로 가로지른다** | 판이 지저분해 읽히지 않는다. 읽히지 않는 판은 추리할 수 없다 |
| **방이 한쪽에 뭉친다** | 뭉친 곳은 겹쳐 보이고 빈 곳은 판을 헛돌게 만든다 |

아래 두 줄이 이번에 추가된 요구사항이고, 그 둘 때문에 파이프라인을 통째로 뒤집었다.

---

## 17.2 연결 차수가 곧 정보량

인접 위험도 합은 **내가 선 방의 연결 차수**에 따라 정보량이 달라진다.
지뢰찾기에서 인접 칸 수가 정보량을 정하는 것과 같다.

| 내 방의 차수 | 보이는 것 |
| --- | --- |
| 1 (막다른 방) | 합 = **그 한 방의 정확한 위험도.** 모호함 없음 |
| 2 | 두 방이 뭉친다 |
| 4+ | 거의 읽을 수 없다 |

**연결을 촘촘하게 짜면 어려워지고 성기게 짜면 쉬워진다.**
몬스터 수치를 건드리지 않고 난이도를 조절하는 손잡이다.
현재 평균 차수는 **2.9 안팎**이다(실측, §17.9).

### 막다른 방은 정찰 지점이다

차수 1인 방에 들어가면 앞방의 위험도를 **정확히** 알 수 있다.
대신 퇴로가 하나뿐이다.

> **정보가 정확한 대신 도망칠 길이 없다.**

위험 대 정보의 트레이드오프가 그래프 모양에서 저절로 나온다.
따라서 생성기는 막다른 방을 **없애지 않는다. 비율만 관리한다.**

**그런데 새 파이프라인에서는 오히려 막다른 방이 모자란다.**
블루 노이즈 위의 근접 그래프가 너무 고르기 때문이다(저절로는 5~10%).
그래서 §17.3 6단계에서 몇 군데를 **일부러 끊는다.** 목표 범위는 14~30% 다.

---

## 17.3 생성 절차 (개정)

```
1. 방 자리 뿌리기      — 푸아송 디스크 샘플링 (블루 노이즈)
2. 삼각분할            — 들로네 (Bowyer-Watson). 교차하지 않는 후보 간선을 얻는다
3. 입구 고르기         — 판 가장자리의 방 하나
4. 고도                — 입구에서의 거리 + 저주파 노이즈
5. 간선 선택           — RNG(+MST) 뼈대에 가브리엘 곁가지. 비용에 고도차와 깊이를 반영
6. 막다른 방 조정      — 비율이 모자라면 끊고 넘치면 잇는다
7. 탈출 경로 파기      — 입구에서 탈출구까지 지형을 바닥까지 깎는다
8. 방 종류 배치        — 필요 민첩과 거리로 귀중품 · 보스 · 탈출구를 놓는다
9. 존재 배치           — 몬스터 · NPC (SampleDungeons)
```

단계마다 파일이 다르다. `src/core/dungeon/generation/` 참고.

| 단계 | 파일 |
| --- | --- |
| 1 | `poisson_disk_sampler.gd` |
| 2 | `delaunay_triangulation.gd` |
| 5, 6 | `proximity_graphs.gd`, `dungeon_edge_selector.gd` |
| 4, 7 | `dungeon_terrain.gd` |
| 8 | `room_kind_planner.gd` |
| 전체 조립 | `dungeon_generator.gd` |

### 17.3.1 왜 순서를 뒤집었는가

이전 파이프라인은 **그래프를 먼저 만들고 나중에 2D 로 펼쳤다.**
층을 나누고, 척추를 놓고, 곁가지를 붙인 뒤, 힘 기반 완화(밀고 당기기)로 좌표를 잡았다.

그 순서에서는 두 문제가 **원리적으로** 풀리지 않는다.

| 증상 | 원인 |
| --- | --- |
| 간선이 서로 가로지른다 | 만들어 놓은 그래프가 평면 그래프라는 보장이 없다. 평면에 그릴 수 없는 그래프는 어떤 레이아웃으로도 교차를 풀 수 없다 |
| 방이 뭉치거나 빈다 | 배치가 연결 관계를 따라가므로 밀도를 직접 정할 수단이 없다 |

순서를 뒤집으면 둘 다 사라진다.

- **자리를 먼저 정하니 밀도를 직접 정한다.** 블루 노이즈가 최소 간격과 빈 구멍 없음을 동시에 준다.
- **후보 간선이 전부 삼각분할에서 나오니 무엇을 골라도 교차하지 않는다.**

두 번째가 특히 중요하다. **교차를 검사해서 걸러 내는 것이 아니라,
교차가 나올 수 없는 후보 집합에서만 고른다.** 실패하는 재시도 루프가 통째로 사라진다.

### 17.3.2 1단계 — 왜 푸아송 디스크인가

| 방식 | 최소 간격 | 빈 구멍 | 무작위 |
| --- | --- | --- | --- |
| 균등 난수 | 없음 (반드시 뭉친다) | 생긴다 | 있다 |
| 격자 | 있다 | 없다 | **없다** (판이 매번 똑같다) |
| **블루 노이즈** | **있다** | **없다** | **있다** |

Bridson 알고리즘을 쓴다. 격자를 깔아 근처 검사를 상수 시간으로 만들기 때문에
방 개수에 선형이다.

**개수를 정확히 맞추지는 못한다.** 목표에서 ±15% 안팎으로 흔들린다.
정확히 맞추려면 뽑은 점을 도로 지워야 하는데, 그러면 방금 보장한 "구멍 없음"이 깨진다.
화면에 "방 N개 안팎"으로 적는 이유다.

영역은 가로세로비 1.45 의 직사각형이다. 정사각형이 더 단순하지만
화면이 가로로 길어서 세로 여백만 남는다.

### 17.3.3 2단계 — 왜 들로네인가

삼각분할은 **정의상 평면 직선 그래프**다. 두 간선은 공유하는 끝점 말고는 만나지 않는다.
따라서 그 부분집합은 무엇을 골라도 교차하지 않는다.

`Geometry2D.triangulate_delaunay()` 를 쓰지 않고 Bowyer-Watson 을 직접 구현했다.
판의 평면성이 이 판정 하나에 걸려 있어 내부를 알고 있어야 하고,
축퇴(네 점이 한 원 위)를 어느 쪽으로 처리할지 우리가 정해야 하기 때문이다.
현재는 축퇴를 "외접원 바깥"으로 처리한다 — 안쪽으로 처리하면 삼각형이 과하게 지워져
구멍의 경계가 별 모양이 아니게 되고, 그러면 재삼각분할이 겹치는 삼각형을 만든다.

### 17.3.4 5단계 — 왜 MST + 무작위 복원이 아닌가

MST 는 **나무**라 순환이 없다. 순환이 없으면 판의 절반이 막다른 길이 되고
되돌아 나오는 길 말고는 선택지가 없어 추리판이 아니라 미로가 된다.
거기에 들로네 간선을 무작위로 되살리면 **모양이 지형과 무관해진다** —
어떤 곳은 삼각형이 빽빽하고 어떤 곳은 한 줄만 남는다.

그래서 중간 단계를 뼈대로 쓴다.

```
MST  ⊆  RNG(상대이웃)  ⊆  가브리엘  ⊆  들로네
```

| 그래프 | 조건 | 쓰임 |
| --- | --- | --- |
| RNG | a 에서도 b 에서도 ab 보다 가까운 제3의 방이 없다 | **뼈대.** 평균 차수 2.5 안팎, 순환 있음, EMST 를 포함해 연결성 보장 |
| 가브리엘 | ab 를 지름으로 하는 원 안에 다른 방이 없다 | **곁가지 후보** |
| MST | — | 연결성의 최후 보루 (부동소수 오차 대비) |

**RNG 를 고른 이유**는 조건이 자리에서 나오기 때문이다.
"둘 사이에 낀 방이 없을 때만 잇는다"는 규칙이라 결과가 자리와 어긋날 수 없고,
사람이 손으로 그린 길처럼 통로가 이웃한 방을 건너뛰지 않고 차례로 이어진다.
그러면서 EMST 를 포함하므로 **도달 가능성이 공짜로 따라온다.**

**곁가지를 가브리엘로 제한한 이유**는 다른 성질이다.
가브리엘 간선은 두 방 사이에 낀 방이 없다는 뜻이라, 그 선을 그어도
**엉뚱한 제3의 방을 스치고 지나가지 않는다.** 교차가 없어도 선이 방을 관통하면
이름도 숫자도 못 읽으므로(§17.6) 이것도 품질 기준이다.

MST 는 이론상 RNG 에 포함되므로 보통 아무것도 더하지 않는다.
부동소수 오차로 RNG 간선 하나가 떨어져 나가는 경우에만 다리를 놓는다.
그 한 번을 위해 검증-재시도 루프를 두는 것보다 이쪽이 싸고 확실하다.

### 17.3.5 곁가지의 비용 — 여기가 레벨 디자인이다

곁가지 후보마다 점수를 매겨 높은 것부터 채택한다.

```
점수 = 완만함 x 얕음 x 흔들림
완만함 = 1 / (1 + 고도차)
얕음   = 1 / (1 + 0.3 x 입구에서의 거리)
흔들림 = 0.55 ~ 1.45 사이의 난수
```

| 항 | 판에 나타나는 결과 |
| --- | --- |
| 완만함 | 가파른 통로가 드물어진다. 흔하면 필요 민첩이라는 등급 자체가 의미를 잃는다 |
| 얕음 | **입구 근처는 촘촘하고 깊은 곳은 성기다.** 첫 턴에 고를 것이 생기고(V7), 깊은 곳에 우회로가 적어져 귀중품이 값을 치르고 닿는 자리가 된다(V3) |
| 흔들림 | 같은 지형에서 늘 같은 곁가지가 나오지 않게 |

**의뢰받은 "방의 중요도와 고도를 고려"가 이 두 항이다.**
중요도를 따로 계산하지 않고 **입구에서의 거리를 대리값으로 쓴다.**
방 종류는 아직 정해지지 않았고(8단계), 깊은 곳이 곧 중요한 곳이기 때문이다.
순환 의존을 만들지 않으면서 같은 결과를 얻는 가장 단순한 방법이다.

### 17.3.6 고도는 지형이지 그래프가 아니다

이전 생성기는 고도를 **층**으로 매겼다. 층을 나누고 층마다 방을 놓는 방식이라
고도가 그래프 구조와 한 몸이었고, 간선을 하나 더 이으면 지형이 통째로 흔들렸다.

지금은 좌표에서 바로 만든다.

```
고도 = 입구에서의 거리 x 0.85 + 노이즈 x 2.6
```

순서가 **지형 먼저, 길은 그 위에** 가 되어 5단계가 고도차를 비용으로 볼 수 있다.
그리고 입구는 언제나 판에서 가장 낮다 — "내려가는 것은 자유"라는 규칙이
입구를 향한 방향과 같은 뜻이 되어야 지도가 읽힌다([`07`](07-level-design.md) §7.2.6.2).

**탑뷰에서 고도는 배치에 쓰지 않는다.** Y 축은 고도가 아니다.
고도를 눈으로 보여 주는 것은 나중에 측면뷰가 생기면 그쪽이 맡는다
([`11-decisions.md`](11-decisions.md) 번복 기록).

---

## 17.4 필요 민첩 (bottleneck)

```
필요 민첩(방) = min over 경로 ( max over 간선 ( 상승폭 ) )
```

내려가는 간선의 상승폭은 0 이다([`07`](07-level-design.md) §7.2.6).
Dijkstra 와 같은 구조에 "합" 대신 "최댓값"을 쓰면 그대로 구해진다.

이 값의 쓰임:

| 용도 | 내용 |
| --- | --- |
| 방 등급 | 배치의 기준 (§17.3-8) |
| 탈출구 보장 | 필요 민첩 0 인 탈출구가 최소 하나 |
| 고가치 배치 | 가치와 필요 민첩을 비례시킨다 |
| (추후) UI | "저기 가려면 민첩이 얼마나 더 필요한가" |

### 17.4.1 탈출구는 거리로 값을 받는다

탈출구는 **민첩으로 막을 수 없다.** 민첩 0 으로 닿는 탈출구가 최소 하나 없으면
시작하자마자 진 판이 되기 때문이다([`07`](07-level-design.md) §7.2.9).

그래서 대가를 다른 축에서 받는다.

| 종류 | 치르는 값 |
| --- | --- |
| 보스 · 귀중품 | **필요 민첩** — 능력이 올라야 손이 닿는다 |
| 탈출구 | **거리** — 판을 가로질러 걸어가야 한다 |

탈출구는 입구에서의 간선 수 거리가 최대의 65% 이상인 방들 중에서 고른다.

---

## 17.5 검증 규칙 — 검사에서 보장으로

**옛 방식: 판을 만들고 검사해서 실패하면 통째로 버리고 다시 만든다.**
그 방식의 문제는 실패율이 파라미터에 민감하다는 점이다. 재시도 상한을 넘기면
파라미터를 완화해야 했고, 완화된 판은 의도한 판이 아니었다.

**새 방식: 각 규칙을 구조로 보장한다.** 생성기에 재시도 루프가 없다.

| # | 규칙 | 어떻게 보장하는가 |
| --- | --- | --- |
| V1 | 모든 방이 입구에서 도달 가능 | 뼈대에 MST 가 들어 있다. 간선을 끊을 때도 끊어 본 뒤 판이 갈라지면 되돌린다 |
| V2 | 필요 민첩 0 인 탈출구가 최소 하나 | 입구에서 탈출구까지의 경로를 바닥까지 깎는다 (§17.3-7) |
| V3 | 고가치 방의 필요 민첩 > 0 | 필요 민첩이 0 보다 큰 방에만 보스 · 귀중품을 놓는다. 자격이 되는 방이 모자라면 그만큼만 놓는다 |
| V4 | 입구의 인접 위험도가 상한 이하 | 배치 단계에서 덜어 낸다 (`SampleDungeons`) |
| V5 | 막다른 방 비율이 14~30% | 모자라면 끊고 넘치면 잇는다 (§17.3-6) |
| V6 | 같은 위험도 합을 만드는 서로 다른 구성이 존재 | 방 종류마다 존재 구성을 다르게 놓는다 (보스방은 큰 하나, 위험방은 작은 여럿) |
| V7 | 입구의 차수가 2 이상 | 모자라면 가장 짧은 들로네 간선을 붙인다 |
| **V8** | **어떤 두 간선도 교차하지 않는다** | **후보가 들로네 부분집합이다.** 검사 자체가 필요 없다 |
| **V9** | **밀도가 고르다** | **푸아송 디스크 샘플링.** 최소 간격과 빈 구멍 없음이 알고리즘의 성질이다 |

### 탈출 경로 파기 (V2)

입구에서 탈출구까지의 최단 경로를 잡아 그 위 방들의 고도를 전부 0 으로 내린다.

부수 효과가 오히려 이득이다 — **판을 가로지르는 낮은 길이 하나 생겨서
"돌아 나올 수 있는 길"이 지도에서 눈에 띈다.** 탈출 지점 위치는 알 수 있어야 한다는
배치 원칙([`07`](07-level-design.md) §7.6)이 지형으로도 표현되는 셈이다.

### 폐기한 것

| 폐기 | 이유 |
| --- | --- |
| 고도 층(layer) 구성 | 고도가 그래프와 한 몸이라 간선을 하나 바꾸면 지형이 통째로 흔들렸다 |
| 척추 + 곁가지 연결 | 평면성을 보장하지 못한다. 교차의 직접 원인 |
| 힘 기반 완화 배치 | 밀도를 통제할 수 없고, 평면 그래프가 아니면 교차도 못 푼다 |
| 검증 실패 시 전량 재생성 | 규칙을 구조로 보장하면 실패할 판이 나오지 않는다 |
| 파라미터 완화 재시도 | 위와 같은 이유. 완화된 판은 의도한 판이 아니다 |

힘 기반 완화는 **좌표가 없는 설계도**(손으로 짠 판, 테스트 픽스처)를 위해
`DungeonBlueprint.layout()` 안에 남아 있다. 생성된 판에는 쓰이지 않는다.

---

## 17.6 자동 레이아웃 — 탑뷰

**좌표는 이제 생성 단계에서 나온다.** 배치는 더 이상 별도의 문제가 아니다.

`DungeonBlueprint.layout(seed, spacing)` 는 저장된 좌표에 간격을 곱해 돌려준다.
단위는 "방 사이 최소 간격 1" 이므로 화면이 간격을 바꾸면 판 전체가 같이 커진다.
현재 화면 간격은 280 px 다(`dungeon_board.gd`).

> **그 좌표에서 간선을 골랐으므로 다른 좌표로 그리면 교차가 되살아난다.**
> 좌표와 연결은 한 몸이다.

### 품질 기준

| 기준 | 상태 |
| --- | --- |
| 간선끼리 교차하지 않는다 | **보장.** 들로네 부분집합 |
| 선이 상관없는 방을 관통하지 않는다 | **보장에 가깝다.** 곁가지를 가브리엘로 제한. 테스트로 간격 0.3 이상 확인 |
| 방이 겹치지 않는다 | **보장.** 최소 간격이 곧 화면 간격 |
| 넓은 빈 구멍이 없다 | **보장.** 어느 지점에서든 간격 1.6 안에 방이 있다 |

"완벽한 교차 제거는 비용에 비해 얻는 게 적다"고 적어 두었던 옛 판단은 **틀렸다.**
비용이 큰 것은 교차를 제거하는 일이지, 교차가 나오지 않게 만드는 일이 아니었다.

### 판은 화면보다 커도 된다

끌어서 이동하고 휠로 확대한다([`07`](07-level-design.md) §7.8).
화면에 맞춰 줄이면 읽히지 않고, 읽히지 않는 판은 추리할 수 없다.

---

## 17.7 재현성

생성은 **시드**를 받는다. 같은 시드는 같은 판을 만든다.

| 이유 | 내용 |
| --- | --- |
| 테스트 | 판을 고정해야 생성 규칙을 검증할 수 있다 |
| 버그 재현 | "이상한 판이 나왔다"를 다시 볼 수 있다 |
| 밸런싱 | 같은 판에서 수치만 바꿔 비교한다 |

Godot 의 `RandomNumberGenerator` 를 인스턴스로 들고 다닌다.
전역 난수(`randi()`)를 쓰면 시드 고정이 깨진다.

**정렬 비교 함수 안에서는 난수를 뽑지 않는다.** 비교 함수가 몇 번 불리는지는
정렬 구현에 달려 있어서, 그 안에서 난수를 뽑으면 같은 시드가 다른 판을 만든다.
점수를 미리 계산해 두고 값으로만 비교한다.

---

## 17.8 수치

전부 잠정이며 굴려 보고 정한다. 정의는 `DungeonGenerator.Params`.

| 항목 | 값 |
| --- | --- |
| 방 개수 | 맵 크기 1~5 에서 12 / 17 / 22 / 27 / 32 (안팎) |
| 곁가지 비율 | 방 개수의 0.24 |
| 막다른 방 비율 | 14% ~ 30% |
| 고도 상승 | 방 하나 거리당 0.85 |
| 지형 기복 | 진폭 2.6, 주파수 0.16 |
| 탈출구 최소 거리 | 최대 거리의 65% |
| 귀중품 비율 | 0.14 |
| 위험방 비율 | 0.16 |

---

## 17.9 실측

`tools/bench_dungeon_generation.gd` 로 잰다 (크기마다 30판).

| 맵 크기 | 방 개수 (최소/평균/최대) | 간선 | 막다른 방 | 생성 시간 (평균/최대) |
| --- | --- | --- | --- | --- |
| 1 | 10 / 12.4 / 14 | 16.8 | 16% | 1.4 ms / 2.0 ms |
| 2 | 15 / 17.4 / 20 | 24.3 | 17% | 2.2 ms / 2.8 ms |
| 3 | 18 / 21.4 / 25 | 30.6 | 16% | 2.9 ms / 3.8 ms |
| 4 | 23 / 26.2 / 29 | 37.9 | 16% | 3.6 ms / 4.4 ms |
| 5 | 26 / 30.9 / 35 | 45.2 | 16% | 4.7 ms / 5.9 ms |

**가장 큰 판도 한 프레임(16.7 ms) 안에 끝난다.**
웹은 단일 스레드라 이 값이 프레임 예산을 넘으면 슬라이더를 움직일 때 화면이 멈칫한다
([`01`](01-constraints.md) §1.2).

판을 눈으로 확인하려면:

```
godot --path . -s res://tools/capture_dungeon_maps.gd -- .shots
```

---

## 17.10 무엇이 테스트로 못 박혀 있는가

헤드리스 통과는 판이 예쁜지 알려 주지 않지만, **판이 규칙을 지키는지는 알려 준다.**

| 성질 | 테스트 |
| --- | --- |
| 어떤 두 간선도 교차하지 않는다 | `test_dungeon_layout.gd` — 5개 크기 x 12개 시드, 선분 교차를 직접 판정 |
| 선이 상관없는 방을 관통하지 않는다 | `test_dungeon_layout.gd` |
| 방이 최소 간격 이상 떨어져 있다 | `test_dungeon_layout.gd`, `test_poisson_disk_sampler.gd` |
| 넓은 빈 구멍이 없다 | 같음. 격자로 훑어 가장 가까운 방까지의 거리를 잰다 |
| 새 배치가 옛 배치보다 고르다 | `test_dungeon_layout.gd` — 같은 판을 두 방식으로 놓고 비교 |
| 모든 방에 도달할 수 있다 (V1) | `test_dungeon_generator.gd`, `test_proximity_graphs.gd` |
| 민첩 0 탈출구가 있다 (V2) | `test_dungeon_generator.gd` |
| 귀중품이 평범한 방보다 비싸다 (V3) | `test_dungeon_generator.gd` |
| 탈출구가 평범한 방보다 멀다 | `test_dungeon_generator.gd` |
| 입구에 갈림길이 있다 (V7) | `test_dungeon_generator.gd` |
| 막다른 방 비율 (V5) | `test_dungeon_generator.gd` |
| 같은 시드 -> 같은 판 | `test_dungeon_layout.gd`, `test_dungeon_generator.gd` |
| 삼각분할이 진짜 들로네다 | `test_delaunay_triangulation.gd` — 외접원 안에 다른 점이 없다 |
| 포함 관계 MST ⊆ RNG ⊆ 가브리엘 ⊆ 들로네 | `test_proximity_graphs.gd` |

**교차 판정기 자체도 테스트한다.** 아무것도 못 잡는 검사기면 위 테스트는 늘 통과한다.

---

## 17.11 현재 파이프라인 재확인 (2026-08-08 조사)

> **이 절은 코드를 다시 읽어 확인한 기록이다.** §17.1~17.10 이 설계 근거이고,
> 여기는 **그 설계가 코드에서 실제로 어떻게 실행되는가**와 **어디가 비어 있는가**를 적는다.
> 이 판(조사판)에서는 코드를 고치지 않았다. 계측 도구 하나만 추가했다.

### 17.11.1 호출 순서 — 실제 코드 기준

`SampleDungeons.create_run(seed, size)` 가 유일한 진입점이다.

```
SampleDungeons.create_run(seed, size)
  ├ params_for_size(size)          room_count = 7 + size*5  (12/17/22/27/32)
  ├ DungeonGenerator.generate()
  │   ├ PoissonDiskSampler.region_for_count()  넓이 = count/0.755, 가로세로비 1.45
  │   ├ PoissonDiskSampler.generate()          Bridson, radius 1, attempts 40
  │   ├ DelaunayTriangulation.edges()          Bowyer-Watson
  │   ├ _pick_entrance()                       네 모서리 중 하나를 뽑아 최근접 방
  │   ├ DungeonTerrain.assign()                거리*0.85 + simplex(2.6, 0.16), 입구=0
  │   ├ DungeonEdgeSelector.select()           아래 17.11.2
  │   ├ _breadth_first() / _pick_exit()        깊이 >= 최대깊이*0.65 인 방 중 무작위
  │   ├ DungeonTerrain.carve_route()           입구->탈출구 BFS 경로 고도를 전부 0 으로
  │   ├ ensure_a_climb_exists()                모든 고도가 0 일 때만 한 방을 2 로
  │   └ _assemble() -> RoomKindPlanner.plan()  종류 배치 + 이름
  ├ _populate()                                종류별 몬스터 배치 (여기부터 절차 생성 밖)
  ├ _trim_entrance_threat()                    입구 인접 합 > 4 면 강한 것부터 제거
  ├ _ensure_entrance_has_something_to_read()   입구 인접 합이 0 이면 위험도 2 하나 투입
  └ _pick_rival_room()                         입구의 인접이 아닌 방 아무 곳에 요한 1명
```

**경계가 하나 있다.** `DungeonGenerator` 는 **구조까지만** 만든다.
위험도(존재 배치)는 `SampleDungeons._populate()` 가 만들고, 그쪽은 절차 생성기가 아니라
고정 규칙 + 확률이다. V4·V6 이 생성기 밖에 있는 이유이자, §17.12 후보 여럿이
`SampleDungeons` 를 건드려야 하는 이유다.

### 17.11.2 간선 선택의 실제 실행 순서

`DungeonEdgeSelector.select()` 는 다섯 단계가 **이 순서로** 돈다.

| # | 함수 | 하는 일 | 되돌릴 수 있는가 |
| --- | --- | --- | --- |
| 1 | `_skeleton()` | RNG ∪ MST | — |
| 2 | `_ranked_pool()` | 가브리엘 나머지를 점수순, 그 뒤에 들로네 나머지 | — |
| 3 | `_add_extras()` | 방 수 x 0.24 개를 붙이되 막다른 방 하한을 침범하면 건너뜀 | 아니오 |
| 4 | `_ensure_entrance_choice()` | 입구 차수 < 2 면 최단 들로네 간선 추가 (V7) | 아니오 |
| 5 | `_relieve_dead_ends()` | 막다른 방 비율 > 0.30 이면 풀에서 이어 준다 (V5 상한) | 아니오 |
| 6 | `_carve_dead_ends()` | 막다른 방 비율 < 0.14 면 끊는다. 최대 6 패스 (V5 하한) | 예 — 끊고 연결 깨지면 복구 |

**주의할 점 두 가지.**

- `_relieve_dead_ends(chosen, pool, used)` 의 `used` 는 `_add_extras` 가 **훑은 인덱스**다
  (채택 개수가 아니라 소비 위치). 하한 때문에 건너뛴 후보는 다시 보지 않는다.
- 6단계 `_carve_once` 는 `_makes_one_dead_end` 로 **차수(2,3+) 쌍만** 자른다.
  차수 4 짜리끼리 이어진 간선은 후보가 되지 않으므로, 촘촘한 한가운데는 절대 뚫리지 않는다.
  깊이 큰 순이 아니라 **`_by_key` 가 내림차순이므로 깊은 쪽부터** 자르는 것이 맞다.

### 17.11.3 고도가 실제로 만들어지는 방식과 그 결과

```
raw   = dist(입구, 방) * 0.85 + simplex(방.x, 방.y) * 2.6      # 좌표 단위 = 최소 간격 1
값    = round(raw), 전체에서 최솟값을 빼고, 입구는 강제로 0
carve = 입구->탈출구 BFS 경로 위의 방은 전부 0
```

노이즈 주파수 0.16 은 **좌표 단위 기준**이라 파장이 방 6개 정도다.
즉 옆방끼리 널뛰지 않고 **판 전체에 완만한 언덕 두어 개**가 생긴다.

`carve_route` 는 BFS 경로 위 방을 전부 0 으로 만든다.
경로 길이가 최대 깊이의 65% 이상이므로, **판을 가로지르는 폭 1 짜리 평지 띠**가 하나 생긴다.
이 띠는 §17.5 가 의도한 대로 되돌아 나올 길이 되지만, 동시에
**그 띠에 닿는 모든 간선의 상승폭을 띠 반대편 방의 고도값과 같게 만든다** — 후술(§17.12).

### 17.11.4 방 종류 배치의 실제 규칙

`RoomKindPlanner.plan()` 의 순서는 이렇다.

1. `required_agility_from(입구)` — bottleneck 최단경로
2. 입구 · 탈출구를 먼저 고정 (탈출구는 생성기가 이미 골라 넘겨준다)
3. 나머지를 `필요민첩 + 홉거리/1000` 내림차순 정렬
4. `_place_valuables` — 그 순서로 훑되 **필요 민첩 0 을 만나는 즉시 break**.
   첫 번째가 보스, 그다음부터 `max(1, 방수*0.14)` 개가 귀중품
5. `_place_hazards` — 남은 방마다 독립적으로 16% 확률

**여기서 나오는 성질이 §17.12 후보 대부분의 출발점이다.**

- 보스는 **언제나 필요 민첩이 판에서 가장 높은 방**이다. 예외가 없다.
- 귀중품은 그 바로 다음 순위들이므로 **보스 근처에 뭉칠 수 있다**
  (필요 민첩은 지형에서 나오고, 지형은 저주파라 근처끼리 값이 비슷하다).
- 필요 민첩이 0 보다 큰 방이 적으면 귀중품이 예산보다 적게 놓인다 (설계된 동작, V3).
- 위험방은 위치와 무관한 순수 확률이므로 **입구 옆에도 놓인다.**
  그 뒤 `_trim_entrance_threat` 이 사후에 걷어낸다.

### 17.11.5 기획 문서와의 대조 — 아직 생성기가 못 하는 것

`07-level-design.md` · `13-information-design.md` 를 읽으며 대조한 결과다.

| 근거 | 요구 | 현재 상태 |
| --- | --- | --- |
| `07` §7.6 | 귀중품과 위험을 **같은 곳에** 둔다 | **못 한다.** 귀중품(TREASURE)과 위험방(HAZARD)은 서로 다른 방이고, 위험방은 확률로 흩뿌린다. 귀중품방의 몬스터는 `SampleDungeons` 가 넣는 "금고 파수꾼" 하나뿐 |
| `07` §7.6 | 귀중품은 **유한해야** NPC 경쟁이 성립 | 개수는 유한하지만 **가치 개념이 없다.** `Room.Kind.TREASURE` 뿐이고 전리품 값이 없다 |
| `07` §7.6 | 탈출 지점 **위치를 알 수 있어야** 한다 | 판에 EXIT 이 그려지므로 성립. 다만 **탈출구는 정확히 하나다** — "개수와 위치가 언제 나갈까의 무게를 결정한다"는 문장에 대해 개수 축이 고정 |
| `07` §7.6 | 위험도 숫자만으로 정답이 나오는 배치 회피 | **부분적.** V6 대비(보스=큰 하나, 위험방=작은 여럿)는 있지만 EMPTY 방의 몬스터가 45% 확률 1~2 라 합의 분포가 좁다 |
| `07` §7.3 | 던전마다 **성격이 달라야** 한다 | **못 한다.** 크기 슬라이더가 `room_count` 하나만 바꾼다. 나머지 파라미터 8개는 모든 던전에서 상수 |
| `07` §7.2.3 | 정보 수단(정찰/스킬)이 유료 | 생성기 밖의 문제. 다만 §17.2 가 말하는 "차수가 곧 정보량"은 생성기가 쥐고 있다 |
| `13` §13.5 | 숫자의 **변화량**이 사람을 알려 준다 | NPC 가 요한 1명뿐이라 변화량 축이 거의 죽어 있다. 생성기가 아니라 배치의 문제 |
| `13` §13.6 | 공짜 정보는 전부 결함이 있어야 한다 | 성립 |
| `05` §5.6 | 조우는 방에서만 | 성립 (통로는 간선일 뿐 상태가 없다) |
| `07` §7.2.6.2 | 들어가긴 쉽고 나오긴 어렵다 | **약하다.** §17.12-B 참고 — carve 된 평지 띠와 낮은 고도값 때문에 실제 상승폭이 작다 |


### 17.11.6 실측 — 지금 판의 모양 (60 시드 x 5 크기)

`tools/survey_dungeon_generation.gd` · `tools/survey_dungeon_routes.gd` 로 쟀다.
크기 3(방 22개 안팎)과 크기 5(방 32개 안팎)만 옮긴다. 전체는 도구를 돌리면 나온다.

| 항목 | 크기 3 | 크기 5 | 표준편차(크기 5) |
| --- | --- | --- | --- |
| 방 / 간선 | 21.6 / 30.8 | 30.9 / 45.2 | 2.0 / 3.5 |
| **순환 계수 (E-V+1)** | **10.2** | **15.3** | 1.9 |
| 평균 차수 | 2.85 | 2.92 | **0.10** |
| 차수 1 (막다른 방) | 16.1% | 15.8% | **0.83** |
| 차수 2 | 23.7% | 19.5% | 5.8 |
| 차수 3 이상 | 60.2% | 64.8% | — |
| 다리(끊으면 판이 갈라지는 간선) | 17.2% | 15.5% | 4.0 |
| 지름 | 7.6 | 8.9 | 0.87 |
| 입구 차수 | 2.17 | 2.15 | **0.36** |
| 얕은 쪽 평균 차수 | 3.39 | 3.43 | 0.28 |
| **깊은 쪽 평균 차수** | **2.04** | **2.07** | 0.32 |
| 상승이 있는 간선 | 61.7% | 62.6% | 6.8 |
| 민첩 2 로 닿는 방 | 84.8% | 87.0% | 15.1 |
| 보스가 판에서 필요 민첩 최고인가 | **100%** | **100%** | **0.00** |
| 탈출구 고도 | **0.00** | **0.00** | **0.00** |
| 귀중·보스가 서로 인접한 비율 | 60.4% | 71.4% | 19.1 |

경로 쪽 (귀중품 + 보스 방 전부).

| 항목 | 크기 3 | 크기 5 |
| --- | --- | --- |
| **대안 경로(간선이 겹치지 않는 둘째 길)가 있는 비율** | **32.9%** | **30.9%** |
| 최단 길이 / 대안 길이 | 4.68 / 6.31 | 5.63 / 7.49 |
| **최단 최대 상승 / 대안 최대 상승** | **3.34 / 3.52** | **3.98 / 4.06** |
| 최단 경로 위 위험도 / 대안 위 위험도 | 6.90 / 9.55 | 7.13 / 11.03 |
| **최단이 상승·위험 둘 다에서 뒤지지 않는 비율** | **50.6%** | **50.5%** |
| 귀중 방 고도의 판 안 백분위 | 75.3 | 77.2 |
| 귀중 방이 막다른 방인 비율 | 52.6% | 50.8% |

### 17.11.7 이 수치가 말하는 것 — 왜 유기적으로 안 느껴지는가

사용자 감상은 "그냥 나열하고 노이즈처럼 잇기만 했다" 였다. 수치가 그것을 뒷받침한다.

**(1) 대안 경로가 있어도 고를 이유가 없다.**
귀중 방의 **31~33% 만** 둘째 길이 있고, 그중 **절반은 최단 경로가 상승·위험 양쪽에서 뒤지지 않는다.**
실제로 선택이 되는 판은 귀중 방 여섯 중 하나꼴이다.
결정적인 것은 상승폭이다 — 최단 3.34 대 대안 3.52 로 **사실상 같다.**
대안은 **더 길고(+35%) 더 위험한데(+38%) 기울기는 같다.** 아무도 고르지 않는다.
"빨리 가는 길 · 돌아가는 길"이 성립하려면 축 하나가 반대로 기울어야 하는데 그런 축이 없다.

**(2) 우회로가 가장 없는 곳이 하필 보상이 있는 곳이다.**
얕은 쪽 평균 차수 3.4 대 **깊은 쪽 2.05.** 깊은 절반은 사실상 나무다.
그런데 보스는 **언제나** 필요 민첩 최고인 방이고(100%, 편차 0), 그런 방은 깊은 쪽에 있다.
버그가 아니라 §17.3.5 의 「얕음」 항이 **설계대로 작동한 결과**다 —
"깊은 곳에 우회로가 적어야 귀중품이 값을 치른다"(V3).
**그 설계와 이번 요구가 정면으로 부딪친다.** 어느 쪽을 택할지가 이번 결정이다.

**(3) 간선은 적지 않다. 오히려 많다.**
순환 계수가 크기 5 에서 15.3 — **방 개수의 0.47 배.**
방 31개 판에 독립적인 우회로가 15개인데 지름은 8.9 다.
**판 어디서든 아홉 걸음이면 반대편에 닿는다.** 길을 찾을 것이 없다.
사용자가 말한 "이어진 길이 많이 없지만 fully connected" 의 정반대다.

**(4) 갈림길 자체는 이미 많다.** 차수 3 이상이 **64.8%.**
요구 ③은 이미 충족되어 있고, **문제는 갈림길의 수가 아니라 갈림길이 전부 똑같다는 것**이다.
어느 방에 서도 서너 갈래고 어느 갈래로 가도 비슷한 곳이 나온다. 균질한 그물망이다.
따라서 ②(간선 줄이기)와 ③(갈림길)의 긴장은 **지금 판에서는 없다** — 줄일 여지가 있다.

**(5) 고도는 보상과 상관이 있다. 다만 관문 역할을 못 한다.**
귀중 방 고도 백분위 **75** — "귀중한 방은 고도가 높다"는 이미 성립한다.
그러나 민첩 2 로 판의 **87%** 가 열린다. 상승이 있는 간선이 62% 인데도 못 막는 이유는
고도가 입구를 꼭짓점으로 하는 **매끈한 원뿔**이라 어디서든 우회해 돌아 올라갈 수 있기 때문이다.
**절벽이 없고 경사만 있다.** 그래서 고도가 지도에 선을 긋지 못한다.

**(6) 가치 높은 방이 판에서 가장 외진 방이다.**
귀중·보스 방의 평균 연결 수가 **1.86** 인데 판 평균 차수는 **3.00** 이다(방 50개 기준, §17.13.1).
절반 이상(51%)이 막다른 방이다.
사용자가 요구한 "가치 높은 방은 주변과 많이 연결되어 위협에 빠진다"의 **정확히 반대**다.

**(7) 늘 똑같은 것들.** 절차 생성이 아니라 상수인 값들이다.

| 값 | 편차 | 왜 상수인가 |
| --- | --- | --- |
| 보스 = 필요 민첩 최고 방 | 0.00 | `RoomKindPlanner` 이 정렬 1순위로 뽑는다 |
| 탈출구 고도 = 0 | 0.00 | `carve_route` 가 경로를 바닥까지 깎는다 |
| 입구 차수 = 2 | 0.36 | V7 이 2 를 만들고 그 이상 붙일 이유가 없다 |
| 막다른 방 = 16% | 0.83 | 하한 14% 에 붙어 있다 |
| 평균 차수 = 2.9 | 0.10 | 곁가지 비율이 상수 0.24 |
| 던전 성격 | — | 크기 슬라이더가 `room_count` 하나만 바꾼다. 나머지 8개는 전 던전 공통 |

**판마다 다른 것은 방의 자리뿐이고 판의 성질은 매번 같다.**
「유기적이지 않다」의 실체가 이것이다 — 시드를 바꿔도 **같은 종류의 판**이 나온다.

---

## 17.12 설계안 — 「랜덤하게 느껴지면 실패」 (2026-08-08 승인, §17.17 로 적용됨)

> **아직 구현하지 않았다.** `src/` 는 손대지 않았다.
> 아래는 `tools/` 의 프로토타입으로 **실제로 돌려 보고 잰 결과까지** 포함한 제안이다.
> 착수 전 승인이 필요하다 (CLAUDE.md 원칙 5).

### 17.12.1 합격 기준을 먼저 못 박는다

의뢰인 확정: **"의도적 레벨디자인이 무조건 들어가야 해. 이게 랜덤하다고만 느껴지면 실패야."**

이것은 있으면 좋은 항목이 아니라 **없으면 실패**하는 조건이다. 그리고 단서가 붙는다 —

> **수치 목표는 필요조건이지 충분조건이 아니다.**
> 순환 계수 · 차수 분포 · 막다른 길 비율을 전부 맞춰도 랜덤하게 느껴질 수 있다.
> 통계가 고른 판은 「고르게 랜덤한 판」이지 「설계된 판」이 아니다.

**설계됐다고 느껴지는 것은 플레이어가 패턴을 읽을 수 있을 때다.**
방을 보고 *"아 여기는 이래서 이렇게 생겼구나"* 가 되어야 한다.
그건 분포가 아니라 **의도의 배치**다. 그래서 §17.12.3 에 **패턴의 어휘**를 정의한다.

### 17.12.2 문제의 재정의

앞 판이 푼 것은 **기하 문제**였다 — 교차와 밀도(§17.5 V8·V9). 그건 됐다.
남은 것은 **레벨 디자인 문제**다. 지금 파이프라인에는 판을 만드는 규칙은 있는데
**무엇이 플레이어의 결정이 되는가**가 없다.

지금 순서: 자리 → 간선 → **그 결과로** 방 종류.
방 종류가 마지막이라 **보상이 구조를 만들지 못하고 구조에 얹힐 뿐이다.**
그래서 보상 근처에 우회로가 있는지가 우연이 된다(실측 31%).

제안 순서: 자리 → **구역** → 구역 사이 관문 → **보상 자리** → **그 보상까지의 두 경로** → 나머지 간선.
**보상을 먼저 놓고 그 주변을 설계한다.**

### 17.12.3 패턴의 어휘 — 이게 이 설계안의 본체다

각 패턴은 **화면에서 알아챌 수 있어야** 한다. 못 알아채는 패턴은 없는 것과 같다.

| # | 패턴 | 무엇을 만드나 | 플레이어가 어떻게 알아채나 | 생성기가 어디에 놓나 |
| --- | --- | --- | --- | --- |
| **P1** | **절벽 봉우리** | 보상 방을 국소 최고점으로 세운다. 이웃 어디서 봐도 올려다본다 | 판에서 고도 숫자가 가장 큰 방인데 **선이 여러 개** 붙어 있다 | 입구에서 가장 먼 구역의 최고점. 판에 하나 |
| **P2** | **급경사 지름길 / 완경사 우회** | 같은 목적지에 성격이 반대인 두 길 | **빨간 선(상승 3+)이 이어진 짧은 사슬**과 **회색·주황 선의 긴 사슬**이 한 방에서 만난다 | P1 과 주 보상에만. 판돈이 없는 곳에는 두지 않는다 |
| **P3** | **노출된 보상** | 보상 방의 차수를 4 이상으로. 들어오는 길이 많다 = 조우가 많다 | 큰 원에 선이 사방으로 뻗어 있다 | P1 과 같은 방. 둘은 겹친다 |
| **P4** | **막다른 보물** | 차수 1 짜리 방에 부차 귀중품 | 판 끝에 **선 하나로 매달린** 방 | 구역의 잎. 높은 구역 것부터 |
| **P5** | **관문(초크)** | 구역 사이를 잇는 통로 하나 | 방 덩어리 둘이 **선 하나로만** 이어져 있다 | 구역 경계마다 하나 |
| **P6** | **저지대 귀환로** | 입구에서 탈출구까지 고도 0 인 띠 | **어두운 원들이 한 줄로** 이어진다 | 이미 있다 (`carve_route`, §17.5 V2) |

**P2 와 P4 는 결정의 종류가 다르다.**
P2 는 「빨리 갈까 돌아갈까」, P4 는 「들어갔다 나올까」다.
둘 다 있어야 판이 단조롭지 않다.

#### P1·P3 의 충돌을 정면으로 푼다

고가치 방이 **연결이 많아야 한다(P3)** 와 **접근이 어려워야 한다(고도)** 를 동시에 요구받는다.

> **길은 많되 전부 험하게.**
> 보상 방을 **봉우리**로 세우면 붙어 있는 간선이 전부 오르막이 된다.
> 차수는 4 이상이라 노출은 최대이고, 필요 민첩은 봉우리 높이만큼 든다.

그리고 §7.2.7 이 이 조합을 이미 무섭게 만들어 뒀다 —
**못 올라가는 방의 것들도 나에게는 내려올 수 있다.** 봉우리 아래에 서면
사방에서 내려올 수 있는 자리에 서는 셈이다. **보상이 곧 노출**이 지형에서 나온다.

예외를 하나 둔다 — **완경사 우회(P2)의 마지막 한 칸만 경사로로 눕힌다.**
그래야 기본 민첩으로도 닿는 길이 하나 남고, 그 길은 멀다.

### 17.12.4 네 가지 수단 (패턴을 만드는 장치)

**A. 구역(zone)과 관문 — 판에 구획을 준다. → P5**
푸아송 점들을 구역으로 나눈다(입구를 첫 씨앗으로 하는 최원점 샘플링 + 최근접 배정).

- **구역 안**은 촘촘하게 → 갈림길이 유지된다
- **구역 사이**는 가장 짧은 들로네 간선 **하나씩만** → 거시 구조가 성기다

②(간선 적게)와 ③(갈림길 많이)의 긴장은 이렇게 푼다 — **성김과 촘촘함을 다른 축척에 둔다.**
지금 판이 균질한 이유는 축척이 하나뿐이기 때문이다.

**B. 구역을 고리로 잇는다 — 「돌아가는 길」의 정체. → P2**
구역 그래프를 나무가 아니라 **고리**로 만든다(구역 중심을 각도 순으로).
입구가 반드시 고리 위에 있고 **어느 구역으로 가든 좌우 두 방향**이 생긴다.
거시 순환 계수가 정확히 1 이다 — 간선 몇 개로 대안을 얻는 가장 싼 방법이다.

> **프로토타입에서 실제로 겪은 함정:** 처음에 구역을 **나무**로 잇고 보상 구역에만
> 고리를 덧댔더니 보스에 대안 경로가 있는 판이 **8~42%** 밖에 안 됐다.
> **입구 구역이 그 고리 위에 없으면** 두 경로가 앞부분을 공유해 결국 한 길이다.
> 고리는 **입구를 포함해야** 뜻이 있다.

**C. 두 경로를 「검사」가 아니라 「시공」한다. → P2**
보상 방을 정한 뒤 입구에서 그 방까지 **간선이 겹치지 않는 두 경로를 만든다.**
없으면 들로네에 남은 미사용 간선으로 **판다.** 들로네 부분집합이라 교차는 여전히 없다(V8).
§17.5 의 「검사에서 보장으로」를 경로에 적용한 것이다.
**프로토타입에서 보스 대안 경로 보유율이 어느 크기에서도 100% 가 됐다** (지금 7~20%).

그리고 **둘의 성격을 벌린다.** 대안이 있기만 해서는 선택이 아니다(§17.11.7-1).

- **급경사 지름길**: 목표 직전 한 칸을 떨어뜨려 상승폭 3 을 만든다 → 민첩으로 값을 치른다
- **완경사 우회**: 한 칸에 1 씩만 오르도록 깎는다 → 기본 민첩으로 걸어 들어간다. 대신 멀다

> **목표 방의 고도를 올리는 방식은 틀렸다.** 두 길이 같은 방으로 들어오므로 양쪽이 똑같이
> 가팔라진다. 프로토타입에서 실제로 그랬다. **길 쪽을 손봐야 한다.**

**D. 고도를 거리가 아니라 구역 등급에서. → P1·P5**
지금은 `거리 x 0.85 + 노이즈` 라 매끈한 원뿔이고 **절벽이 없다**(§17.11.7-5).
구역 등급 x 단차 + 낮은 노이즈로 바꾸면 **구역 안은 평탄하고 경계에서만 층이 진다.**

> **여기서 두 번 틀렸고 그 기록을 남긴다.**
> 단차를 3 으로 두면 관문마다 벽이 서서 기본 민첩으로 갈 수 있는 방이 **20%** 까지 떨어진다.
> 고도 폭을 고정하고 구역 수로 나누면 이번엔 **작은 판만** 가팔라진다(구역이 적어 단차가 커진다).
> **고도로 판 전체를 막는 것이 애초에 틀린 접근이다.**
> 지형은 걸어 다닐 수 있게 두고(단차 2, 기본 민첩으로 넘는다),
> **벽은 보상 봉우리와 지름길에만 세운다.** 그래야 벽이 있는 곳이 곧 의도가 된다.

### 17.12.5 배치 원칙 — 패턴이 아무 데나 있으면 그것도 랜덤이다

| 원칙 | 뜻 |
| --- | --- |
| **판돈이 걸린 곳에만** | P2(두 경로)는 보스와 주 보상에만. 빈방 사이에 우회로가 있어 봐야 선택이 아니다 |
| **한 판에 하나씩** | P1 봉우리는 판에 하나. 둘이면 어느 쪽이 목적지인지 읽히지 않는다 |
| **패턴 수는 방 개수에 비례** | P4 막다른 보물 = 방수 x 0.06, P5 관문 = 구역 수. 판이 커지면 같이 는다 |
| **패턴끼리 겹치는 곳이 절정** | P1 + P2 + P3 이 같은 방에서 만난다. 그 방이 이 판의 주제다 |

### 17.12.6 목표 수치 — 필요조건

| 항목 | 지금 (방 50) | 목표 | 근거 |
| --- | --- | --- | --- |
| 순환 계수 | 24.2 (0.52 x 방수) | **0.25~0.30 x 방수** | 우회로 하나 = 고리 하나. 방 50개 판에 24개는 셀 수 없다. 12~15개면 「이 판에 우회로가 몇 군데」가 인지된다 |
| 평균 차수 | 3.00 | 2.5~2.6 | 위와 같은 말 |
| 차수 3 이상 | 69.1% | **55~60%** | 지금도 과하지 않다. 줄이되 절반 아래로는 내리지 않는다 |
| 막다른 방 | 15.2% | 14~22% | P4 를 쓰려면 여유가 필요하다 |
| 지름 | 11.5 | **15~18** | 관문을 지나야 하므로 길어진다. 「길을 찾는 재미」의 직접 지표 |
| 보상 방 대안 경로 | 7.5% | **보스 100%** | 시공하므로 보장된다 |
| 두 경로 상승폭 차 | 0.3 (없음) | **1.5 이상** | 축 하나가 반대로 기울어야 선택이 된다 |
| 민첩 2 로 열리는 방 | 77.4% | 70~80% | 지형은 다닐 수 있어야 한다. 벽은 P1·P2 에만 |
| **보상 방의 차수** | **1.86** | **4 이상** | P3. 지금은 판 평균(3.00)보다도 낮다 |

**어느 것이 비례하고 어느 것이 상수인가** (방 50개 요구에 답한다).

| 비례해야 하는 것 | 상수여야 하는 것 |
| --- | --- |
| 순환 계수 (x 방수) | 차수 분포 비율 |
| 구역 수 (방 6개당 1, 3~8) | 막다른 방 비율 |
| 막다른 보물 개수 (x 방수) | 봉우리 1개 · 두 경로 쌍 1~2개 |
| 지름 (구역 수를 통해 간접적으로) | 입구 차수 3 |

**봉우리와 두 경로 쌍을 상수로 두는 것이 핵심이다.** 이것까지 비례시키면
판이 커질수록 절정이 여러 개가 되어 어디가 이 판의 주제인지 읽히지 않는다.

### 17.12.7 프로토타입 실측 — 제안이 실제로 무엇을 바꾸는가

`tools/proto_organic_dungeon.gd` (제품 코드 아님) + `tools/survey_dungeon_scale.gd`.
같은 시드, 같은 목표 방 개수 **50**, 40판씩.

| 항목 (목표 방 50개) | 지금 | 프로토타입 | 목표 | 판정 |
| --- | --- | --- | --- | --- |
| 실제 방 | 46.2 | 46.2 | — | 같다 (샘플러 공유) |
| 간선 | 69.4 | 57.9 | — | ○ |
| **순환 계수** | 24.2 | **12.7** | 12~15 | ○ |
| 평균 차수 | 3.00 | 2.51 | 2.5~2.6 | ○ |
| 차수 1 | 15.2% | 14.0% | 14~22 | ○ |
| **차수 3 이상** | 69.1% | **45.3%** | 55~60 | **× 미달** |
| **지름** | 11.5 | **14.7** | 15~18 | △ 근접 |
| 민첩 2 로 열리는 방 | 77.4% | 76.6% | 70~80 | ○ |
| **보스 대안 경로 보유** | 7.5% | **100%** | 100 | ○ 보장 |
| **보상 방 차수** | 1.86 | 보스 4 / 전체 평균 1.54 | 4 이상 | △ 보스만 |
| 두 경로 최대 상승 (지름길/우회) | 4.36 / 4.64 | 4.28 / 4.15 | 차 1.5+ | **× 미달** |
| 최단이 더 완만하기까지 한 비율 | 65.1% | 47.5% | 낮을수록 | △ |
| 생성 시간 (생성기만) | 7.04 ms | 5.58 ms | 16.7 미만 | ○ |

**정직하게 적는다. 프로토타입이 다 푼 것은 아니다.**

| 남은 문제 | 원인 (확인함) | 어떻게 풀 것인가 |
| --- | --- | --- |
| 차수 3 이상이 45% | 구역마다 허브를 **하나만** 두고 곁가지를 그 허브에만 붙였다 | 허브를 구역당 2개로. 밀도는 구조와 독립된 손잡이다 |
| 두 경로 상승폭 차가 작다 | 완경사 우회를 깎은 뒤 봉우리를 세우는데, 봉우리가 우회 경로의 마지막 칸도 함께 끌어올린다 | 고도 편집을 **한 번의 순서 있는 통과**로 정리한다. 지금은 세 곳(`_sharpen` · `_carve` · 노이즈)이 같은 값을 덮어쓴다 |
| 보상 방 평균 차수 1.54 | P4(막다른 보물)가 평균을 끌어내린다. **P3 과 P4 는 같은 방에서 동시에 성립할 수 없다** | 지표를 나눈다 — 보스·주 보상은 차수 4 이상, 부차 귀중품은 차수 1. **평균을 보지 말고 종류별로 본다** |
| 지름 14.7 (목표 15~18) | 구역 수 상한 8 | 상한을 10 으로. 다만 구역이 작아지면 구역 안 갈림길이 준다 (위 첫 줄과 상충) |
| 존재(위험도) 배치는 손대지 않았다 | `SampleDungeons` 는 생성기 밖이다 (§17.11.1) | 별개 과제. §17.11.5 의 `07` §7.6 대조표 참고 |

### 17.12.8 지금 구조와 충돌하는가 · 비용

**충돌하지 않는다. 갈아엎지 않는다.**
푸아송 → 들로네 → 근접 그래프 → 선택은 그대로다. 후보 간선이 여전히 들로네 부분집합이므로
**V8(교차 없음)과 V9(밀도)는 손대지 않아도 유지된다.** 바뀌는 것은 **무엇을 고르느냐**다.

| 단계 | 바뀌는가 |
| --- | --- |
| 1 푸아송 · 2 들로네 · 3 입구 | 그대로 |
| 4 고도 | **바뀐다** — 거리 원뿔 → 구역 등급 + 노이즈, 벽은 국소적으로 |
| 5 간선 선택 | **바뀐다** — 「얕음 x 완만함」 점수 → 구역 안/밖 규칙 + 경로 시공 |
| 6 막다른 방 조정 | 유지. 하한만 조정 |
| 7 탈출 경로 파기 | 그대로 (P6) |
| 8 방 종류 | **순서가 앞으로 온다** — 보상 자리를 간선 선택 전에 정한다 |
| 9 존재 배치 | 이번 범위 밖 |

**앞 레인의 판단(RNG ∪ MST + 가브리엘)을 뒤집지 않는다.**
그 근거였던 "MST 는 나무라 판의 절반이 막다른 길이 된다"(§17.3.4)는 여전히 옳고,
이번 요구 ②·③과도 같은 방향이다. 구역 **안**은 그대로 근접 그래프를 쓴다.
바뀌는 것은 **구역 사이**를 어떻게 잇느냐뿐이다.

**비용.** 구역 나누기는 방 개수 x 구역 수, 경로 시공은 BFS 몇 번이다.
방 50개에서 생성기만 재면 지금 7.04 ms, 프로토타입 5.58 ms 로 **오히려 빠르다**(간선이 적다).
다만 `SampleDungeons._populate` 는 여기 포함되지 않았으므로 **구현 후 다시 재야 한다.**

90시간 안에 되는가 — **A~D 는 프로토타입 규모로 이미 돌아간다.**
실제 비용은 §17.10 의 테스트 표를 그만큼 늘리는 쪽이다.

---

## 17.13 방 50개로 올리면 (2026-08-08 조사)

### 17.13.1 지금 생성기의 스케일 (`tools/survey_dungeon_scale.gd`, 40판씩)

| 목표 방 | 실제 방 | 간선 | 순환 계수 | 평균 차수 | 차수 3+ | 지름 | 민첩 2 | 보스 대안 | 보상 차수 | 생성 ms |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 12 | 12.7 | 17.1 | 5.5 | 2.71 | 53.2% | 5.2 | 90.1% | 20.0% | 1.82 | 1.21 |
| 22 | 21.9 | 31.6 | 10.7 | 2.89 | 61.1% | 7.6 | 86.3% | 17.5% | 1.84 | 2.63 |
| 32 | 30.6 | 45.1 | 15.6 | 2.95 | 64.7% | 8.8 | 80.7% | 5.0% | 1.85 | 4.12 |
| **50** | **46.2** | **69.4** | **24.2** | **3.00** | **69.1%** | **11.5** | **77.4%** | **7.5%** | **1.86** | **7.04** |
| 64 | 58.5 | 89.5 | 32.1 | 3.06 | 71.0% | 12.7 | 80.8% | **0.0%** | 1.93 | 10.16 |

**판이 커질수록 문제가 커진다.**
차수 3 이상이 53% → 71% 로 오르고 순환 계수가 방 개수의 0.52 배까지 간다.
그런데 **보스에 대안 경로가 있는 판은 20% → 0% 로 사라진다.**
큰 판일수록 더 균질한 그물망이 되고 동시에 보상은 더 외길이 된다.
**50개는 지금 생성기의 약점이 가장 크게 드러나는 크기다.**

### 17.13.2 무엇이 무너지는가

| 항목 | 판정 |
| --- | --- |
| **기획 문서가 방 개수를 못 박았는가** | **아니다.** `07` §7.3 의 던전 목록 표는 **비어 있다.** 방 개수를 정한 문장이 없다. 지금 12~32 는 `SampleDungeons.params_for_size` 의 `7 + size*5` 일 뿐 근거가 있는 수가 아니다 |
| **생성 시간** | 7.04 ms (생성기만, 방 46개). 프레임 예산 16.7 ms 안이다. **다만 64개에서 10.16 ms 라 여유가 없다.** `_populate` 와 UI 노드 생성은 아직 안 잰 값이다 |
| **화면** | `dungeon_board` 는 끌어서 이동 · 휠 확대를 이미 한다(`07` §7.8). 방 46개면 판이 지금의 1.5배다. **확대 범위와 초기 배율이 충분한지는 미확인** — 창을 띄워 확인해야 하고 이번 판에서는 하지 않았다 |
| **지표 스케일** | §17.12.6 의 표대로. 순환 계수 · 구역 수 · 막다른 보물은 **비례**, 차수 분포 비율 · 봉우리 1개 · 두 경로 쌍은 **상수** |
| **읽히는가** | 방 46개에서 인접 위험도 합만으로 추리가 성립하는지는 **미확인.** 정보량이 방 수에 비례해 늘지 않으므로(§17.2) 판이 커지면 추리가 아니라 노동이 될 수 있다. **굴려 봐야 안다** |

**결론: 50개는 기술적으로 문제없다. 다만 지금 생성기로 50개를 만들면 균질함이 더 심해진다.**
개수를 올리는 것과 §17.12 를 넣는 것은 **같이 가야 한다.**

### 17.13.3 눈으로 보는 법 — 숫자를 얹은 판

```
godot --headless --path . -s res://tools/draw_dungeon_svg.gd -- .shots
```

`.shots/dungeon_compare.html` 을 브라우저로 연다.
**같은 시드 4개를 방 50개로, 위(지금)·아래(설계안)** 로 그린다. 창을 띄우지 않는다.

| 그림에서 | 뜻 |
| --- | --- |
| 원 **안의 숫자** | 그 방의 고도 |
| 원의 **크기** | 방의 가치 (보스가 가장 크다) |
| 원의 **밝기** | 고도 (숫자를 읽지 않고 지형만 훑을 때) |
| 테두리 **색** | 방 종류 |
| 간선의 **화살촉과 숫자** | 올라가는 **방향**과 **상승폭** |
| 선의 색·굵기 | 상승폭 (회색 0 · 주황 1~2 · 빨강 3+) |
| 굵은 주황 / 초록 띠 | 빠른 길 / 돌아가는 길 (보스 방까지) |

**밝기와 색만으로는 검산이 안 된다.** 처음에는 고도를 원의 밝기로, 상승폭을 선의 색으로만
칠했다. 그러면 "대충 저쪽이 높다"까지만 읽히고 **두 경로를 따라가며 더해 보는 것이 불가능하다.**
「급경사 지름길 / 완경사 우회」가 이 설계안의 핵심 약속인데 그림이 그것을 증명하지 못하면
보는 사람은 측정값을 믿는 수밖에 없다. 그래서 숫자를 얹었다.

**평지 간선에는 화살표를 그리지 않는다.** 전부 그리면 무엇이 오르막인지가 오히려 흐려진다.
대신 판마다 머리글에 **평지가 몇 %인지** 적는다.

**두 경로는 지금 생성기 쪽에도 같은 방법으로 계산해 그린다.** 프로토타입에만 그리면
비교가 되지 않는다. 그리고 그렸더니 이렇게 나왔다.

| 시드 (방 50개) | 지금 — 빠른 길 / 돌아가는 길 | 설계안 — 빠른 길 / 돌아가는 길 |
| --- | --- | --- |
| 11 | 7칸 상승 8 / **없음** | 10칸 상승 **3** / 14칸 상승 **1** |
| 23 | 10칸 상승 8 / **없음** | 4칸 상승 **4** / 10칸 상승 **1** |
| 47 | 11칸 상승 9 / **없음** | 5칸 상승 **4** / 11칸 상승 **1** |
| 91 | 11칸 상승 9 / **없음** | 6칸 상승 **4** / 14칸 상승 **2** |

**지금 생성기는 네 판 모두 돌아가는 길이 없다.** 그리고 하나뿐인 그 길이 상승폭 8~9 다 —
스쿼드 기본 민첩이 2 인데 보스 방에 닿으려면 8 이 필요하고, 우회로는 없다.
§17.14.2 의 「보스 차수 1.08」과 같은 사실의 다른 얼굴이다.

설계안은 네 판 모두 **짧고 가파른 길(상승 3~4)과 길고 완만한 길(상승 1~2)** 이 함께 있다.
**이 표는 실측 평균이 아니라 그림에 적힌 값 그대로다.** 그림에서 직접 세어 검산할 수 있다.

### 17.13.4 계측 · 시각화 도구

| 도구 | 재는 것 |
| --- | --- |
| `tools/survey_dungeon_generation.gd` | 지금 판의 모양 32항목. 평균과 **표준편차**(무엇이 상수인가) |
| `tools/survey_dungeon_routes.gd` | 귀중 방까지의 두 경로 · 고도와 방 종류의 상관 |
| `tools/survey_dungeon_scale.gd` | 방 개수를 올렸을 때. 지금 것과 프로토타입을 한 표에 |
| `tools/survey_proto_dungeon.gd` | 프로토타입을 크기 슬라이더 기준으로 |
| `tools/dungeon_metrics.gd` | 위 도구들이 공유하는 지표 정의 (도구마다 다르게 재면 비교가 무의미해진다) |
| `tools/draw_dungeon_svg.gd` | 판을 SVG 로. 지금 것과 설계안을 나란히 |
| `tools/proto_organic_dungeon.gd` | 설계안 프로토타입 본체 (**제품 코드 아님**) |
| `tools/survey_dungeon_params.gd` | 파라미터를 흔들면 판이 얼마나 달라지나 + 성격이냐 운이냐 (§17.16) |
| `tools/check_board_capacity.gd` | 화면이 방 N 개를 담는가 (헤드리스, §17.15) |
| `tools/capture_board_50.gd` | 방 50개 판을 실제 화면에 올려 배율 세 개를 한 번에 찍는다 |

---

## 17.14 미달 셋을 좁힌 기록 (2026-08-08, 프로토타입만)

> §17.12.7 이 남긴 미달 세 줄을 프로토타입 안에서 좁혔다. **`src/` 는 여전히 손대지 않았다.**
> 여기 적는 것은 결과보다 **왜 틀렸는가**다. 같은 실수를 구현 때 다시 하지 않기 위해서다.

### 17.14.1 두 경로 상승폭 차 — 0.13 → 1.85 (목표 1.5+, 달성)

**이것이 설계안의 핵심 약속이었다.** 급경사 지름길과 완경사 우회의 차이가 0.13 이면
「빨리 갈까 돌아갈까」가 아예 없는 것이다. 세 번 틀리고 고쳤다.

| # | 무엇을 했나 | 왜 틀렸나 |
| --- | --- | --- |
| 1 | 목표 방의 고도를 올렸다 | 두 길이 **같은 방으로 들어온다.** 양쪽이 똑같이 가팔라진다 |
| 2 | 우회로를 "한 칸에 1 씩" 으로 깎았다 | 경사로가 **시작하는 지점**에서 앞 칸과의 차이가 그대로 절벽이 됐다 |
| 3 | 봉우리를 세운 뒤 계단을 맞췄다 | 경로가 짧거나 탈출 통로와 겹치면 계단이 목표에 못 미쳐 **마지막 한 칸이 절벽**이 됐다 |

**고친 방법은 순서를 뒤집는 것이었다.**

```
(예전) 봉우리를 세운다 -> 계단을 맞춰 본다 -> 안 맞으면 절벽이 남는다
(지금) 계단이 닿을 수 있는 높이를 먼저 잰다 -> 봉우리를 거기에 맞춘다
```

우회로를 앞에서부터 훑어 「여기까지는 1 씩 올라오면 몇까지 닿는가」(`reach`)를 구하고,
봉우리 높이를 `min(주변+3, reach+1)` 로 정한 뒤 계단을 거꾸로 놓는다.
그러면 **우회로의 최대 상승폭이 1 임이 계산이 아니라 구성으로 보장된다.**
§17.5 의 「검사에서 보장으로」가 고도에도 적용된 셈이다.

그리고 **고도를 건드리는 곳을 한 군데로 모았다.** 예전에는 `_sharpen` 뒤에 `_carve` 가 와서
방금 세운 경사로를 탈출 통로가 도로 0 으로 깎았다. 지금은 깎는 것을 먼저 하고,
깎인 칸을 `protected` 로 넘겨 계단이 그 칸을 피하되 **거기서부터 다시 시작**한다.

| 방 50개 | 지금 생성기 | 프로토타입 |
| --- | --- | --- |
| 지름길 최대 상승 | 4.36 | **4.08** |
| 우회로 최대 상승 | 4.64 | **2.23** |
| **차이** | **-0.28 (우회가 더 가파르다)** | **+1.85** |
| 최단이 더 완만하기까지 한 비율 | 65.1% | **20.0%** |

지금 생성기는 **대안이 더 가파르기까지 하다.** 길고, 위험하고, 가파르다. 고를 이유가 없다.

### 17.14.2 P3 와 P4 를 갈라 재기 — 진단이 맞았다

한 평균으로 재면 **둘 다 실패로 보인다.** 노출된 보상(차수 4+)과 막다른 보물(차수 1)은
서로 반대 방향이라 섞으면 가운데 값이 나온다. 종류별로 나눠 재니 그림이 완전히 달라졌다.

| 방 50개 | 지금 생성기 | 프로토타입 | 목표 |
| --- | --- | --- | --- |
| **보스 방 차수** | **1.08** | **4.20** | 4 이상 |
| 부차 귀중품 차수 | 1.99 | **1.00** | 1 |

**지금 생성기의 보스 차수가 1.08 이다.** 평균 1.86 보다도 낮다 —
보스는 거의 언제나 **막다른 방**이다. 판 평균 차수가 3.00 인데 이 판의 주인공만 외길이다.
§17.11.7-6 에서 잡은 것보다 더 심하다.

프로토타입에서는 보스가 차수 4 이면서 **봉우리**라 붙은 간선이 전부 오르막이다.
「길은 많되 전부 험하게」(§17.12.3)가 수치로 성립한다.

### 17.14.3 갈림길 45.3% → 50.4% (목표 55~60, 미달)

구역마다 허브를 하나만 두면 구역 안이 그 방을 지나는 **별 모양**이 된다.
둘로 늘리니 갈림길이 두 군데 생겼다. 다만 간선이 함께 늘어 순환 계수가 목표를 넘었고,
허브 곁가지 채택 확률을 0.75 -> 0.62 로 조여 되돌렸다.

**둘은 같은 손잡이의 양끝이다.** 갈림길을 늘리면 순환이 늘고, 순환을 조이면 갈림길이 준다.
50.4% 는 목표에 5%p 못 미치지만 **더 밀면 순환 계수가 목표를 벗어난다.**
허브를 셋으로 늘리는 대신 **허브끼리 잇는 간선을 우선 채택**하는 쪽이 남은 방법이다
(같은 간선 수로 차수를 더 몰아 준다). 미검증이다.

### 17.14.4 방 50개 최종 (프로토타입, 40판씩)

| 항목 | 지금 | 프로토타입 | 목표 | 판정 |
| --- | --- | --- | --- | --- |
| 간선 / 순환 계수 | 69.4 / 24.2 | 59.9 / **14.7** | 12~15 | ○ |
| 평균 차수 | 3.00 | **2.59** | 2.5~2.6 | ○ |
| 차수 1 | 15.2% | 12.3% | 14~22 | △ 조금 낮다 |
| 차수 3 이상 | 69.1% | 50.4% | 55~60 | △ 미달 |
| 지름 | 11.5 | **14.4** | 15~18 | △ 근접 |
| 보스 대안 경로 | 7.5% | **100%** | 100 | ○ 보장 |
| 두 경로 상승폭 차 | -0.28 | **+1.85** | 1.5+ | ○ |
| 최단이 더 완만한 비율 | 65.1% | **20.0%** | 낮을수록 | ○ |
| 보스 차수 / 귀중품 차수 | 1.08 / 1.99 | **4.20 / 1.00** | 4+ / 1 | ○ |
| 민첩 0 탈출구 (V2) | 100% | **100%** | 100 | ○ 유지 |
| 민첩 2 로 열리는 방 | 77.4% | 95.5% | 70~80 | **× 목표와 충돌** |
| 생성 시간 (생성기만) | 7.26 ms | **3.78 ms** | 16.7 미만 | ○ |

### 17.14.5 목표 하나를 철회한다 — 「민첩 2 로 열리는 방 70~80%」

§17.12.6 에서 이 목표를 세웠는데, **§17.12.4-D 에서 내린 결론과 앞뒤가 맞지 않는다.**
거기서 "고도로 판 전체를 막는 것이 애초에 틀린 접근이다. 지형은 걸어 다닐 수 있게 두고
벽은 보상 봉우리와 지름길에만 세운다" 고 적었다. 그러면 **열리는 방은 많아야 정상이다.**

70~80% 는 「고도가 관문 역할을 못 한다」(§17.11.7-5)는 진단에서 반사적으로 세운 수치였다.
그 진단의 진짜 내용은 **비율이 아니라 위치**였다 — 막힌 곳이 아무 데나 있어서 읽히지 않는다는 것.

> **바꿔 다는 지표:** 「민첩 2 로 열리는 방의 비율」 대신
> **「보상 봉우리에 민첩 2 로 닿을 수 있는가 (닿으면 실패)」** 를 본다.
> 판 전체가 열려 있어도 **주인공 방 하나가 잠겨 있으면** 고도는 제 역할을 한 것이다.

프로토타입은 봉우리를 주변+3 으로 세우므로 이 지표는 구조적으로 만족한다.
**다만 아직 지표로 만들어 재지는 않았다 — 미확인이다.**

### 17.14.6 아직 안 한 것

| 항목 | 상태 |
| --- | --- |
| 화면이 방 50개를 감당하는가 | **미확인.** `dungeon_board` 의 끌기 범위 · 확대 하한을 창 띄워 봐야 한다 |
| 던전별 파라미터로 성격 주기 | **미착수** (§17.11.7-7 의 "파라미터 9개 중 8개가 전 던전 공통") |
| 차수 1 이 12.3% 로 낮다 | 허브 둘 + 곁가지가 잎을 먹는다. P4(막다른 보물) 자리가 줄어든다 |
| 존재(위험도) 배치 | 범위 밖. `SampleDungeons` 는 생성기 밖이다 (§17.11.1) |

---

## 17.15 화면이 방 50개를 감당하는가 (2026-08-08 조사)

> `tools/check_board_capacity.gd` (헤드리스) + `tools/capture_board_50.gd` (창 한 번).
> **`src/` 는 고치지 않았다.** 무엇을 고쳐야 하는지까지만 적는다.

### 17.15.1 실측 — 지도의 크기와 담기는 배율

기준 화면 1280x720, 간격 280, 방 위젯 146x76, 확대 하한 0.40. 방 개수마다 시드 5개.

| 목표 방 | 실제 방 | 지도 크기 (배율 1) | 판 전체가 들어오는 배율 | **하한에서 보이는 넓이** | 최소 간격 | 전체를 볼 때 이름 |
| --- | --- | --- | --- | --- | --- | --- |
| 12 | 11.4 | 1323 x 926 | 0.778 | 100% | 98 px | 보인다 |
| 22 | 22.6 | 1926 x 1302 | 0.553 | 100% | 95 px | 안 보인다 |
| 32 | 30.2 | 2269 x 1540 | 0.468 | 100% | 92 px | 안 보인다 |
| **50** | **45.0** | **2845 x 1931** | **0.373** | **86.9%** | **92 px** | 안 보인다 |
| 64 | 59.4 | 3225 x 2189 | 0.329 | 67.6% | 88 px | 안 보인다 |

**방 50개에서 처음으로 판 전체가 화면에 들어오지 않는다.** 필요한 배율이 0.373 인데 하한이 0.40 이다.
32개까지는 100% 들어왔다. **이 문제는 50개에서 새로 생긴다.**

### 17.15.2 이름과 숫자는 겹치지 않는다 — 전제가 깨지지 않았다

`dungeon_board.gd` 의 간격 주석("방 위젯보다 넉넉해야 이름이 겹쳐 보이지 않는다")이
개수가 늘면 깨질까 걱정했는데 **깨지지 않는다.**

가장 가까운 두 방의 위젯 사이 간격이 방 12개에서 98 px, 64개에서 88 px 다. 거의 변하지 않는다.
**푸아송 디스크가 최소 간격을 보장하기 때문이다** — 방이 늘면 영역이 같은 밀도로 넓어질 뿐
이웃 사이가 좁아지지 않는다(§17.3.2). 밀도는 개수에 대해 불변이다.

> **간격 전제는 방 개수와 무관하다.** 이건 블루 노이즈로 뒤집은 파이프라인이
> 의도하지 않게 준 이득이다. 예전 힘 기반 배치였다면 개수가 늘 때마다 뭉쳤을 것이다.

### 17.15.3 창을 띄워 본 것 — 숫자가 말해 주지 않는 것 셋

방 44개(시드 11)를 실제 화면에 올려 배율 1.0 / 0.62 / 0.40 세 장을 한 번의 실행으로 찍었다.
**이번에는 그림을 눈으로 봤다** (§17.13.3 의 SVG 검증과 달리 수치 검증이 아니다).

**(1) 하한 배율에서 방은 「내용」이 아니라 「무늬」다.**
0.40 에서 방 위젯은 58x30 px 다. `RoomNode` 는 이 배율에서 이름만 감추고 숫자는 남기는데
(`Detail.MINIMAL`), 실제로는 **하프톤 망점이 칸을 채워 숫자가 묻힌다.**
`07` §7.8 이 약속한 "낮음 = 숫자만 보이는 배율"이 지켜지지 않는다.

**(2) 방 종류가 색으로 구분되지 않는다.**
같은 §7.8 이 낮은 배율을 "판의 **모양과 색**을 읽는 배율"이라고 적었는데,
화면에서 보스방과 빈방이 **똑같이 생겼다.** 색이라고 부를 것이 없다.
이건 방 개수와 무관한 문제이고, 50개에서 더 아프게 드러날 뿐이다.

**(3) HUD 가 지도를 덮는다 — 계산에 안 들어간 손실이다.**
좌측 정보판(약 290x290), 좌하단 「송출중」 명판(약 370x70), 우하단 조작바(약 540x70)가
지도 위에 얹힌다. 합치면 뷰포트의 **약 16%** 다.
지도의 86.9% 만 들어오는데 그중 16% 가 가려지므로 **실효로 보이는 판은 약 73%** 다.

한편 **이동으로는 판 구석까지 갈 수 있다.** `_clamp_pan` 이 화면 절반만큼 패딩을 두어
가장자리 방도 화면 가운데로 끌어올 수 있다. **못 보는 것이 아니라 한눈에 못 보는 것이다.**

### 17.15.4 「안 들어와도 된다」는 전제가 50개에서도 유효한가

`dungeon_board.gd` 주석과 `07` §7.8 은 이렇게 적어 뒀다.

> 판은 한 화면에 다 들어오지 않아도 된다. 지도가 화면에 맞춰 줄어들면 방이 뭉개져
> 오히려 읽기 어렵다.

**절반만 유효하다.** 같은 §7.8 이 배율마다 하는 일을 따로 정해 뒀기 때문이다.

| 배율 | §7.8 이 정한 일 | 50개에서 |
| --- | --- | --- |
| 높음 | "여기서 어디로 갈까" | 성립 |
| 낮음 | **"지금 판이 어떻게 생겼나"** | **성립하지 않는다** |

**「판이 어떻게 생겼나」를 보려면 판이 화면에 들어와야 한다.**
플레이 배율에서 안 들어오는 것은 의도지만, **훑어보는 배율에서 안 들어오는 것은 결함이다.**
32개까지는 우연히 둘 다 성립해서 이 모순이 드러나지 않았다.

> **판단: 결함이 맞다.** 다만 "화면에 맞춰 줄인다"가 답은 아니다.
> 주석의 경고("줄이면 뭉개진다")는 §17.15.3-(1)에서 실제로 확인됐다 —
> 이미 0.40 에서 뭉개지고 있다. **더 줄이는 방향은 막혀 있다.**

### 17.15.5 선택지와 대가

| # | 안 | 방 50개에서 | 대가 | 비용 |
| --- | --- | --- | --- | --- |
| A | 확대 하한 0.40 -> 0.33 | 판이 들어온다 | **방 위젯이 48x25 px.** 이미 0.40 에서 못 읽는데 더 작아진다. 윤곽만 남는다 | 상수 1개 |
| B | **방 간격 280 -> 240** | **판이 들어온다 (필요 배율 0.432)** | 최소 간격 92 -> **58 px.** 여전히 안 겹친다 | 상수 1개 |
| C | 간격 280 -> 200 | 넉넉히 들어온다 | 최소 간격 **29 px.** 이름이 붙어 보이기 시작한다 | 상수 1개 |
| D | 아무것도 안 한다 | 87% 만 보인다 | 「판의 모양을 읽는 배율」이 없어진다 | 0 |
| E | HUD 를 지도 밖으로 | 실효 시야 +16% | 레이아웃 재설계. 화면이 좁아진다 | 큼 |

**간격을 줄였을 때의 실측** (방 50개, 시드 5개).

| 간격 | 지도 크기 | 필요 배율 | 하한에서 보이는 넓이 | 최소 간격 |
| --- | --- | --- | --- | --- |
| 280 (지금) | 2845 x 1931 | 0.373 | 86.9% | 92 px |
| **240** | **2460 x 1666** | **0.432** | **100%** | **58 px** |
| 220 | 2267 x 1534 | 0.470 | 100% | 44 px |
| 200 | 2074 x 1401 | 0.514 | 100% | 29 px |
| 180 | 1881 x 1269 | 0.568 | 100% | 15 px |

### 17.15.6 적용 — 간격 280 -> 240 (2026-08-08, 반영됨)

**B 를 적용했다.** `dungeon_board.gd` 의 `_SPACING` 하나를 240 으로 내렸다.
사용자가 방 50개를 요청했고 **판을 못 보면 그 요청이 통째로 막히므로**, 설계안 승인과
별개로 필요한 변경이라고 판단했다. **생성기 본체(`src/core/dungeon/`)는 손대지 않았다.**

적용 후 실측 (같은 도구, 같은 시드).

| 목표 방 | 지도 크기 | 판 전체가 들어오는 배율 | **하한에서 보이는 넓이** | 최소 간격 | 전체를 볼 때 이름 |
| --- | --- | --- | --- | --- | --- |
| 12 | 1155 x 805 | 0.896 | 100% | 63 px | 보인다 |
| 22 | 1672 x 1127 | 0.639 | 100% | 67 px | **보인다** |
| 32 | 1966 x 1331 | 0.541 | 100% | 64 px | 안 보인다 |
| **50** | **2460 x 1666** | **0.432** | **100%** | **58 px** | 안 보인다 |
| 64 | 2785 x 1887 | 0.382 | 91.0% | 64 px | 안 보인다 |

- **방 50개가 확대 하한에서 전부 들어온다.** 목표 달성
- **덤으로 방 22개(지금 기본 크기 3)에서 판 전체를 보면서 이름까지 읽힌다.**
  간격 280 에서는 필요 배율 0.553 이 이름 표시 경계(0.62) 아래라 이름이 사라졌다
- 최소 간격은 58~67 px 로 남는다. 위젯 너비 146 의 40% 이상이라 겹치지 않는다
- **방 64개는 여전히 91% 다.** 사용자 요청은 50개이므로 그대로 둔다.
  64개까지 담아야 하면 간격을 220 까지 더 내려야 하고, 그때 최소 간격이 44 px 로 줄어든다

**A(확대 하한 내리기)는 쓰지 않았다.** 이미 0.40 에서 방이 무늬로 보이므로(§17.15.3-1)
윤곽만 남는 배율을 하나 더 만드는 것은 「모양을 읽는 배율」을 만드는 것이 아니다.

#### 아직 남은 요구 — 낮은 배율에서 방 종류가 구분되어야 한다

판이 100% 담겨도 **보스방과 빈방이 똑같이 생겼으면** 「지금 판이 어떻게 생겼나」는
여전히 읽히지 않는다(§17.15.3-2). `07` §7.8 이 "모양과 **색**"이라고 적어 둔 절반이 비어 있다.

**여기서 색을 정하지 않는다.** 이건 생성기가 아니라 화면의 과제이고, 조형 언어는
UI 킷 쪽에서 나온다([`20-ui-kit.md`](20-ui-kit.md)). **요구만 남긴다.**

> **요구:** `Detail.MINIMAL` 배율에서 방 종류(입구 · 탈출 · 보스 · 귀중품 · 위험 · 빈방)가
> 서로 구분되어야 한다. 수단은 색이든 형태든 UI 킷이 정한다.

### 17.15.7 확인 방법과 그 한계

| 확인한 것 | 방법 |
| --- | --- |
| 지도 크기 · 담기는 배율 · 최소 간격 | 헤드리스 계산 (`check_board_capacity.gd`). 배치 계산의 결과라 그림이 필요 없다 |
| 하한 배율에서 방이 무늬로 보인다 | **창을 띄워 PNG 를 눈으로 봤다** (`capture_board_50.gd`) |
| HUD 가 덮는 넓이 | 같은 PNG 에서 눈으로 재고 픽셀로 어림했다. **정밀 측정이 아니다** |
| 웹 빌드에서도 같은가 | **미확인.** 브라우저 캔버스 크기가 다르면 결론이 달라질 수 있다 |
| 간격 240 적용 후 회귀 | 임포트 · GUT 409 테스트 · gdlint · gdformat · 글리프 전부 통과. **GUT 스크립트 수 36 이 `test/unit` 파일 수 36 과 일치**하므로 파싱 실패로 조용히 건너뛴 테스트는 없다 |

창은 **한 번만** 띄웠고 세 배율을 그 안에서 몰아 찍었다.

**계측 도구가 화면 상수를 베껴 적던 것은 없앴다.** 처음에는 간격 · 확대 하한 · 위젯 크기를
`check_board_capacity.gd` 안에 적어 뒀는데, 그러면 화면이 바뀔 때 도구가 옛 수치로
**조용히 거짓말을 한다.** 실제로 간격을 240 으로 내리면서 두 곳을 고쳐야 했다.
지금은 `DungeonBoard._SPACING` 을 직접 읽고(GDScript 의 밑줄은 관례일 뿐이라 상수는 읽힌다),
위젯 크기는 `room_node.tscn` 을 한 번 세워서 읽는다. **한 곳만 고치면 된다.**

---

## 17.16 「늘 똑같은 것」을 흔든다 — 던전마다 성격 (2026-08-08, 적용됨)

> `tools/survey_dungeon_params.gd` · 그림은 `.shots/dungeon_characters.html`.
> **`src/` 는 고치지 않았다.** 파라미터는 이미 `DungeonGenerator.Params` 로 노출돼 있어
> 던전별로 다른 값을 주는 것은 배선 문제이지 생성기 문제가 아니다.

### 17.16.1 두 증상은 같은 병이다

§17.11.7 에서 두 가지를 찾았다.

| 증상 | 내용 |
| --- | --- |
| 랜덤한 곳은 다 똑같이 랜덤하다 | 시드를 바꿔도 **같은 종류의 판**이 나온다 |
| 의미 있어야 할 곳은 다 똑같이 고정돼 있다 | 보스 = 필요민첩 최고 방(편차 0) · 탈출구 고도 = 0(편차 0) · 입구 차수 = 2 · 파라미터 9개 중 8개가 전 던전 공통 |

**둘 다 「읽을 게 없다」로 귀결된다.** 그리고 §7.3 이 이미 답을 적어 뒀다 —
"던전끼리 강함만 다르면 선택이 아니라 순서다."

### 17.16.2 성격이냐 운이냐 — 경계를 수로 놓는다

던전마다 파라미터를 달리 주면 **「이 던전은 저 던전과 다르다」**가 되어야 한다.
그런데 **같은 던전 안에서 시드만 바꿔도 그만큼 흔들리면** 플레이어에게는
**「이번 판은 운이 나빴다」**로 읽힌다. 전자는 성격이고 후자는 불공정이다.

그 경계를 이렇게 잰다.

```
신호대잡음 = |축을 높였을 때 - 낮췄을 때| / (같은 파라미터에서 시드만 바꿨을 때의 표준편차 x 2)
```

**1 보다 작으면 그 축은 성격이 될 수 없다.** 시드가 더 크게 흔들기 때문에
플레이어는 파라미터 차이를 운으로 돌린다.

### 17.16.3 실측 — 어느 축이 실제로 읽히는가

방 50개 목표, 축마다 낮음 / 지금 / 높음, 시드 24개.

> **아래는 덩어리 2~3 을 적용한 뒤 잰 값이고, 현행은 §17.16.10 이다.**
> 옛 값(구역 없는 생성기)은 §17.16.9 에 있다.
> **구조가 바뀔 때마다 다시 재야 한다** — 이 표만 세 판이다.

| 축 | 낮음 -> 높음 | 가장 크게 움직인 지표 | 축 폭 | 시드 폭 | **신호대잡음** |
| --- | --- | --- | --- | --- | --- |
| 귀중품 비율 | 0.05 -> 0.30 | 귀중품 개수 | 11.6 | 0.6 | **21.0** |
| 막다른 방 하한 | 0.06 -> 0.30 | 막다른 방 % | 19.1 | 2.0 | **9.31** |
| 곁가지 비율 | 0.08 -> 0.48 | 평균 차수 | 0.8 | 0.1 | **6.42** |
| **구역 단차** | 1.0 -> 4.0 | 보스 필요 민첩 | 8.8 | 3.8 | **2.33** |
| 위험방 비율 | 0.05 -> 0.36 | 위험방 개수 | 12.9 | 6.2 | **2.08** |
| **탈출구 최소 거리** | 0.30 -> 0.95 | 탈출구까지 홉 | 5.2 | **6.9** | **0.75** |
| **층 안 기복(진폭)** | 0.4 -> 2.4 | 민첩 2 로 열리는 방 % | 28.5 | **32.7** | **0.87** |
| **기복의 잔 정도(주파수)** | 0.10 -> 0.44 | 보스 필요 민첩 | 0.3 | 3.8 | **0.07** |

**축 하나가 탈락했다 — 탈출구 최소 거리(1.9 -> 0.75).**
축을 흔든 폭은 4.2 -> 5.2 홉으로 오히려 늘었는데 **시드 폭이 2.2 -> 6.9 홉으로 세 배가 됐다.**
구역과 관문이 판을 길게 만들면서(지름 11.5 -> 17.9) 탈출구까지의 거리가 시드에 따라
크게 흔들리게 됐기 때문이다. **판이 길어진 대가로 「탈출구가 먼 던전」을 성격으로 쓸 수 없게 됐다.**

> **이것이 §17.16.2 의 식이 실제로 하는 일이다.** 축을 흔든 폭만 보면 그 축은 더 세졌다.
> 시드 폭과 견주지 않으면 **탈락한 축을 계속 성격으로 쓰게 된다.**

「탈출구가 먼 던전」을 되살리려면 거리가 아니라 **개수**를 흔들어야 한다(§17.16.6 의 신규 항목).
개수는 시드에 흔들리지 않는다 — 하나는 하나고 셋은 셋이다.

**여섯 축은 시드 잡음을 넘고 둘은 못 넘는다.**

- **층 안 기복은 흔들면 안 된다.** 축 폭 28.5%p 인데 **시드 폭이 32.7%p 다.**
  던전 성격으로 쓰면 플레이어는 그걸 운으로 돌린다. 게다가 구역 단차와 **같은 지표**를
  움직이므로, 둘을 함께 흔들면 어느 것이 원인인지 읽히지 않는다.
  **고도 축은 「구역 단차」 하나만 쓴다.**
- **기복의 잔 정도는 아무것도 하지 않는다.** 신호대잡음 0.07 이고, 가장 크게 움직인 지표가
  보스 필요 민첩 0.3 이다(시드 폭 3.8). 흔들면 잡음만 는다.
- **탈출구 최소 거리는 이제 못 쓴다.** 위 표의 근거대로 0.75 다.

몇 가지 축이 서로 얽혀 있는 것도 확인했다.

| 얽힘 | 내용 |
| --- | --- |
| 귀중품 <-> 위험방 | 귀중품 비율을 0.05 -> 0.30 으로 올리면 위험방이 6.0 -> 4.2 로 준다. `_place_hazards` 가 **남은 방**에만 돌기 때문이다 |
| 곁가지 <-> 보스 필요 민첩 | 곁가지를 0.08 -> 0.48 로 올리면 보스 필요 민첩이 7.3 -> 5.0 으로 내린다. 우회로가 늘어 낮은 경로가 생긴다 |
| 막다른 방 <-> 민첩 | 하한을 0.06 -> 0.30 으로 올리면 민첩 2 로 열리는 방이 84.3% -> 71.5% 로 준다 |

**즉 축은 독립이 아니다.** 던전 하나에서 여러 축을 흔들면 서로를 상쇄해
결국 "그냥 다른 판"이 된다.

### 17.16.4 규칙

| # | 규칙 | 근거 |
| --- | --- | --- |
| R1 | **한 던전에서 흔드는 축은 둘까지** | 셋 이상이면 어느 것이 그 던전의 성격인지 읽히지 않는다. 봉우리를 판에 하나만 두는 것과 같은 논리(§17.12.5) |
| R2 | **신호대잡음 1.5 미만인 축은 쓰지 않는다** | 지형 기복(0.53) · 기복의 잔 정도(0.12) 제외 |
| R3 | **파라미터는 던전별로 고정. 시드는 자리만 바꾼다** | 입장 전에 알 수 있어야 성격이고, 입장 후에 드러나면 운이다. 선택이 곧 난이도 선택(§7.3) |
| R4 | **흔든 축이 화면에 보여야 한다** | 아래 표의 「무엇으로 읽히나」가 빈 축은 쓰지 않는다 |
| R5 | 공정성 규칙은 흔들지 않는다 | 민첩 0 탈출구(V2) · 입구 인접 위험도 상한(V4) · 입구 차수 2 이상(V7). 이건 성격이 아니라 최소 조건이다 |

### 17.16.5 축이 화면에서 무엇으로 읽히는가 (R4)

| 축 | 무엇으로 읽히나 |
| --- | --- |
| 방 개수 | 판의 크기. 화면에 "칸 N개 안팎"으로 이미 적힌다 |
| 곁가지 비율 | **선이 촘촘한가 성긴가.** 그리고 인접 위험도 합이 뭉치는 정도(§17.2) |
| 막다른 방 하한 | **판 끝에 선 하나로 매달린 방이 몇 개인가.** 정찰 지점의 수 |
| 고도 상승 | 방에 적힌 **고도 숫자의 크기**와 "오름 N" 표시의 빈도 |
| 탈출구 최소 거리 | **탈출구가 판의 어디에 있는가.** 코앞인가 반대편인가 |
| 귀중품 비율 | 귀중품 방의 개수 |
| 위험방 비율 | 위험방의 개수 |
| ~~지형 기복~~ | 고도 숫자가 들쭉날쭉한 정도. **시드 잡음과 구분되지 않는다** |
| ~~기복의 잔 정도~~ | **읽히는 것이 없다** |

### 17.16.6 「편차 0」 셋에 대한 판단

| 지금 | 흔들 것인가 |
| --- | --- |
| 보스 = 필요 민첩 최고 방 (편차 0) | **규칙은 유지하고 세기만 던전별로.** "가장 가기 어려운 방이 보스"는 **읽히는 규칙**이라 없앨 이유가 없다. 대신 고도 상승 축으로 그 어려움의 크기를 던전마다 달리 준다 (보스 필요 민첩 2.8 ~ 15.3 으로 움직인다) |
| 탈출구 고도 = 0 (편차 0) | **유지.** 민첩 0 탈출구는 공정성 규칙(V2)이다 (R5). 흔들 것은 **개수**다 — `07` §7.6 이 "탈출 지점의 개수와 위치가 「언제 나갈까」의 무게를 결정한다"고 적었는데 지금은 **언제나 하나**다. **생성기에 탈출구 개수 파라미터가 없다 — 신규 항목이다** |
| 입구 차수 = 2 | **유지.** V7 의 하한이고 공정성 규칙이다 (R5). 3~4 로 올리는 것은 효과가 작아 우선순위 낮다 |

**그리고 보스가 치르는 값 자체를 던전 성격으로 쓸 수 있다.**
§17.4.1 이 이미 두 축을 정의해 뒀다 — 보스·귀중품은 **필요 민첩**, 탈출구는 **거리**.
지금은 이 배정이 고정이다. 던전에 따라 **보스를 거리로 막는 것**도 성격이 된다
("능력이 되면 갈 수 있지만 멀다" 대 "가깝지만 못 올라간다").
**이건 `RoomKindPlanner` 를 고쳐야 하므로 신규 항목이다.**

### 17.16.7 그림

```
godot --headless --path . -s res://tools/draw_dungeon_svg.gd -- .shots
```

`.shots/dungeon_characters.html` — **다섯 던전을 같은 시드로 그린다.**
시드를 같이 두는 것이 요점이다. 판이 달라 보이는 이유가 운이 아니라 **파라미터**임을
한눈에 보이게 한다.

| 던전 | 흔든 축 | 그림에 찍힌 값 (시드 11) |
| --- | --- | --- |
| 얕은 갱도 | 방 14 · 곁가지 0.10 · 고도 상승 0.35 | 방 17 · 간선 21 · 고리 5 · 최고 고도 **3** |
| 벌집 | 방 34 · 곁가지 0.46 | 방 32 · 간선 52 · **고리 21** · 최고 고도 7 |
| 수직 회랑 | 방 24 · 고도 상승 1.60 | 방 23 · 간선 34 · 고리 12 · **최고 고도 10** |
| 먼 출구 | 방 40 · 탈출구 거리 0.95 | 방 40 · 간선 59 · 고리 20 · 최고 고도 8 |
| 금고층 | 방 28 · 귀중품 0.05 · 위험방 0.36 | 방 27 · 간선 42 · 고리 16 · 최고 고도 7 |

**한 시드로는 고도 축이 깨끗하게 갈리지 않는다.** 수직 회랑(최고 고도 10)과
먼 출구(8)가 가까운데, 먼 출구는 고도를 흔들지 않았다. 신호대잡음 2.2 라는 값이
"평균으로는 갈리지만 한 판으로는 덜 갈린다"는 뜻이고, 그림이 그것을 보여 준다.
**연결 밀도 축(고리 5 대 21)은 한 판에서도 확연하다** — 신호대잡음 4.4 다.

### 17.16.8 아직 안 한 것 · 신규 항목

| 항목 | 상태 |
| --- | --- |
| 던전별 파라미터 배선 | **미착수.** 지금은 `SampleDungeons.params_for_size` 가 크기 슬라이더에서 `room_count` 하나만 만든다. 던전 정의를 데이터로 두는 자리가 없다 |
| 탈출구 개수 파라미터 | **신규.** 생성기에 없다 |
| 보스가 치르는 값(민첩/거리) 선택 | **신규.** `RoomKindPlanner` 변경 필요 |
| NPC 수 · 성격 | 범위 밖. `SampleDungeons` 는 생성기 밖이다 (§17.11.1) |
| 실제 던전 목록 확정 | `07` §7.3 에 후보를 적었다. **수치는 잠정이고 승인 대기다** |

---

### 17.16.9 옛 값 — 구역 없는 생성기에서 (기록)

**구조가 바뀌면 축의 값어치도 바뀐다.** 아래는 덩어리 2~3 이전(구역·관문 없음, 고도가
거리 원뿔)의 값이다. 지금은 무효이고 비교용으로만 남긴다.

| 축 | 신호대잡음 (옛) | 신호대잡음 (지금) | 무엇이 바뀌었나 |
| --- | --- | --- | --- |
| 귀중품 비율 | 21.0 | 21.0 | 그대로 |
| 막다른 방 하한 | 18.8 | 9.31 | 시드 폭이 1.0 -> 2.0 |
| 곁가지 비율 | 4.4 | 6.42 | 시드 폭이 0.2 -> 0.1 로 줄어 오히려 세졌다 |
| 고도 상승 -> **구역 단차** | 2.2 | 2.33 | 뜻이 바뀌었다(거리당 -> 등급당). 값어치는 비슷 |
| 위험방 비율 | 2.6 | 2.08 | 시드 폭이 4.9 -> 6.2 |
| **탈출구 최소 거리** | 1.9 | **0.75** | **탈락.** 판이 길어져 시드 폭이 세 배 |
| 지형 기복 -> 층 안 기복 | 0.53 | 0.87 | 여전히 잡음 |
| 기복의 잔 정도 | 0.12 | 0.07 | 여전히 잡음 |

---

### 17.16.10 덩어리 4 뒤에 다시 잰 값 (2026-08-08, 현행)

**§17.16.3 의 값은 덩어리 2~3 뒤에 잰 것이고 아래가 현행이다.** 배선하기 전에 다시 쟀다 —
덩어리 4 가 구조를 또 바꿨기 때문이다(지름 17.9 -> 15.5, 순환 15.6 -> 17.4).

| 축 | 덩어리 2~3 뒤 | **덩어리 4 뒤** | 가장 크게 움직인 지표 | 판정 |
| --- | --- | --- | --- | --- |
| 귀중품 비율 | 21.0 | **28.15** | 귀중품 개수 | ○ |
| 막다른 방 하한 | 9.31 | **14.63** | 막다른 방 % | ○ |
| 곁가지 비율 | 6.42 | **4.97** | 평균 차수 | ○ |
| 위험방 비율 | 2.08 | 2.08 | 위험방 개수 | ○ |
| **탈출구 최소 거리** | **0.75** | **1.68** | 탈출구까지 홉 | **○ 되살아났다** |
| **구역 단차** | 2.33 | **1.42** | 민첩 2 로 열리는 방 % | **△ 기준(1.5) 아래** |
| 층 안 기복 | 0.87 | 0.48 | 민첩 2 로 열리는 방 % | x 잡음 |
| 기복의 잔 정도 | 0.07 | 0.11 | 평균 차수 | x 잡음 |

**두 축이 서로 반대로 움직였고 둘 다 덩어리 4 가 원인이다.**

#### 탈출구 최소 거리가 되살아났다 (0.75 -> 1.68)

덩어리 4 에서 탈출구를 고르는 방법이 바뀌었다. 보상 방을 비켜 가는 길이 있는 후보 중
**가장 먼 것**을 결정적으로 고른다(`_pick_exit_clear_of`). 예전에는 자격을 갖춘 후보 중
난수로 뽑았다. **시드 폭이 6.9 -> 2.8 홉으로 줄었다.**

> **성능을 고치려고 넣은 결정적 선택이 성격 축을 되살렸다.**
> 축이 죽은 원인은 판이 길어진 것이 아니라 **고르는 방식이 시드에 흔들린 것**이었다.

`07` §7.3.1 의 「먼 출구」를 **보류에서 되살릴 수 있다.**

#### 구역 단차가 기준 아래로 떨어졌다 (2.33 -> 1.42)

**보스 필요 민첩이 단차와 무관해졌다.** 표를 보면 단차 1.0 / 2.0 / 5.0 에서 보스 필요 민첩이
전부 1.9~2.0 이다. 봉우리를 「주변보다 +3」으로 세우므로(덩어리 4) **보스 난이도가 지형이
아니라 봉우리 상수에서 나온다.**

그래서 이 축이 움직이는 지표가 바뀌었다 — **보스가 어렵다**가 아니라 **판이 잠긴다**로.

| 단차 | 민첩 2 로 열리는 방 | 보스 필요 민첩 |
| --- | --- | --- |
| 1.0 | 95.3% | 1.9 |
| 2.0 (지금) | 82.0% | 2.0 |
| 5.0 | 68.1% | 1.9 |

축 폭 27.3%p 는 크지만 **민첩 2 로 열리는 방의 시드 폭이 18.0%p** 라 신호대잡음이 낮다.
값을 1.0 대 4.0 으로 두면 1.42, **1.0 대 5.0 으로 벌리면 1.52 로 기준을 겨우 넘는다.**

> **축을 살리는 방법은 둘이다 — 시드 폭을 줄이거나 값을 더 벌리거나.**
> 탈출구는 앞쪽으로 살아났고, 단차는 뒤쪽으로만 살릴 수 있다.

#### 뜻이 바뀐 파라미터 점검

덩어리 2·3·4 가 구조를 세 번 바꿨다. **값만 두고 넘기면 의도가 조용히 뒤집히므로** 전부 봤다.

| 파라미터 | 뜻이 바뀌었나 | 조치 |
| --- | --- | --- |
| `elevation_gain` | **바뀜** (거리당 -> 구역 등급당, 덩어리 3) | 이미 고쳤다. 다만 **읽히는 결과가 또 바뀌었다** — 이제 보스가 아니라 판 전체의 통행성을 움직인다 |
| `treasure_ratio` | **효과가 바뀜** (덩어리 4) | 보스가 더 이상 이 예산에서 한 칸을 먹지 않고, 귀중품이 막다른 방부터 채워진다. 개수는 비슷하다(0.05/0.14/0.30 -> 2.0/6.0/13.3) |
| `exit_distance_ratio` | 뜻은 그대로 | 고르는 방식이 결정적으로 바뀌어 **편차만 줄었다** |
| `dead_end_ratio_min` | 뜻은 그대로 | 보정이 위상의 마지막으로 옮겨져 **더 정확히 지켜진다**(신호대잡음 9.31 -> 14.63) |
| `extra_edge_ratio` | 뜻은 그대로 | 뼈대가 작아져 **같은 값이 예전보다 성긴 판**을 만든다. 방향은 그대로다 |
| `elevation_amplitude` · `elevation_frequency` | — | 둘 다 잡음. 성격으로 쓰지 않는다 |

---

## 17.17 설계안을 옮기는 순서 (2026-08-08, 다섯 덩어리 전부 완료)

> 사용자가 §17.12 설계안을 승인했다. `tools/proto_organic_dungeon.gd` 에서 검증된 것을
> `src/core/dungeon/` 으로 옮긴다. **한꺼번에 옮기지 않는다.**

### 17.17.1 순서를 정한 기준 — 되돌리기 쉬운 순서

무언가 깨졌을 때 **어디까지 되돌릴지 알 수 있어야** 한다. 그래서 이렇게 나눴다.

| 덩어리 | 무엇을 옮기나 | 동작이 바뀌는가 | 되돌리는 법 | 확인할 보장 |
| --- | --- | --- | --- | --- |
| **1** | **구역 원시자료** — 구역 분할 · 경계 · 고리 · 등급 · 관문을 순수 함수로 추가 | **바뀌지 않는다.** 아직 아무도 부르지 않는다 | 파일 하나 삭제 | 없음 (기존 경로를 건드리지 않는다) |
| 2 | 간선 선택이 구역을 쓴다 — 구역 안 RNG∪MST + 허브 곁가지, 구역 사이 관문 하나, 고리 | 바뀐다 | `DungeonEdgeSelector` 를 옛 경로로 | **V1** (도달) · **V5** (막다른 방) · **V7** (입구 차수) · **V8** (교차) |
| 3 | 고도를 구역 등급에서 만든다 | 바뀐다 | `DungeonTerrain.assign` 을 거리 원뿔로 | **V2** (민첩 0 탈출구) · **V3** (고가치 민첩) |
| 4 | 보상 자리를 간선 선택 **전에** 정하고 두 경로를 시공 · 봉우리와 경사로 | 바뀐다 | `DungeonGenerator.generate` 의 호출 순서 | **V2** · **V3** · **V8** |
| 5 | 던전별 파라미터 배선 (§7.3 다섯) | 바뀐다 | `SampleDungeons` 를 크기 슬라이더만으로 | 없음 (파라미터만) |

**1번을 먼저 두는 이유**는 동작을 하나도 바꾸지 않으면서 **가장 밑에 깔리는 것**이기 때문이다.
2~4 가 전부 구역 위에 서므로, 구역 계산이 틀리면 뒤의 셋이 다 틀린다.
**틀릴 수 있는 것을 아무것도 안 바꾸는 상태에서 테스트로 못 박아 두는 것**이 가장 싸다.

**2번을 3번보다 먼저 두는 이유**는 고도를 구역 등급에서 만드는 것이 **관문이 있을 때만 뜻이 있기** 때문이다.
관문 없이 계단만 만들면 층 경계를 아무 간선이나 넘어가 「절벽이 아무 데나 있는 판」이 된다.

**5번을 마지막에 두는 이유**는 파라미터 배선이 생성기의 모양에 달려 있기 때문이다.
지금 옮기면 2~4 를 하면서 두 번 고치게 된다.

### 17.17.2 옮기면서 지키는 것

| 규율 | 내용 |
| --- | --- |
| 앞 레인의 판단 | **구역 안에서는 RNG∪MST + 가브리엘을 그대로 쓴다** (§17.3.4). 바뀌는 것은 구역 **사이** 뿐이다 |
| 결정론 | 전역 `randi()` 를 부르지 않는다. 시드 받은 `RandomNumberGenerator` 를 넘긴다 (§17.7) |
| 정렬 | 비교 함수 안에서 난수를 뽑지 않는다. 점수를 미리 계산해 값으로만 비교한다 (§17.7) |
| 교차 없음 | 새로 잇는 간선도 **전부 들로네 부분집합**에서만 고른다 (V8) |
| 이름에 묶지 않기 | §7.3 다섯 던전의 이름과 성격은 기획이라 뒤집힐 수 있다. **구조는 이름을 모른다** |

### 17.17.3 진행

| 덩어리 | 상태 |
| --- | --- |
| 1 구역 원시자료 | **완료** (`dungeon_zones.gd` + `test_dungeon_zones.gd`) |
| 2 간선 선택 | **완료** (`dungeon_edge_selector.gd` · `dungeon_zones.gd`)  |
| 3 고도 | **완료** (`dungeon_terrain.gd`) |
| 4 경로 시공 | **완료** (`dungeon_route_builder.gd` · `dungeon_terrain.gd` · `room_kind_planner.gd`) |
| 5 파라미터 배선 | **완료** (`dungeon_catalog.gd` · `gate.gd` · `sample_dungeons.gd`) |

### 17.17.13 덩어리 4 — 보상 자리를 먼저 정하고 두 경로를 시공

**사용자 요구의 본체다.** 처음 든 예 둘이 여기서 결판난다.

> "귀중한 방은 고도가 높은데 빨리 가는 길 있고 돌아가는 길 있다던지"
> "가치가 높은 방은 주변 방과 많이 연결되어 있어 위협에 크게 빠질 수 있거나"

**바뀐 순서**

```
(예전) 자리 -> 간선 -> 고도 -> 그 결과로 방 종류
(지금) 자리 -> 구역 -> 간선 -> 고도 -> 보상 자리 -> 노출 -> 두 경로 시공
       -> 막다른 방 보정 -> 탈출 경로 깎기 -> 봉우리와 경사로 -> 방 종류
```

| 새 단계 | 하는 일 | 파일 |
| --- | --- | --- |
| `pick_prize` | 자격을 갖춘 방만 후보에 넣고 그중 가장 높은 곳 | `dungeon_route_builder.gd` |
| `expose` | 보상 방의 차수를 4 이상으로 (P3) | 같음 |
| `ensure_two_routes` | 간선이 겹치지 않는 두 경로를 **없으면 판다** | 같음 |
| `enforce_dead_end_floor` | V5 하한을 **위상의 마지막에** 다시 맞춘다 | `dungeon_edge_selector.gd` |
| `sharpen` | 봉우리 + 완경사 계단 + 급경사 지름길 | `dungeon_terrain.gd` |
| `plan(boss_id)` | 보스 자리를 생성기에서 받는다. 귀중품은 막다른 방 우선 (P4) | `room_kind_planner.gd` |

### 17.17.14 실측 — 사용자 요구 셋이 목표에 닿았나 (방 50개, 40판)

| 항목 | 옛 생성기 | 덩어리 3 | **덩어리 4** | 목표 | 판정 |
| --- | --- | --- | --- | --- | --- |
| **보스 대안 경로** | 7.5% | 17.5% | **100%** | 100 | **○ 시공하므로 보장** |
| **보스 방 차수** | 1.08 | 1.38 | **3.67** | 4 이상 | △ 0.33 미달 |
| 부차 귀중품 차수 | 1.99 | 2.15 | **1.01** | 1 (P4) | ○ |
| **두 경로 상승폭 차** | -0.28 | -0.09 | **+1.06** | 1.5 이상 | △ 미달 |
| 최단이 더 완만하기까지 한 비율 | 65.1% | 44.7% | **26.3%** | 낮을수록 | ○ |
| 차수 3 이상 | 69.1% | 54.2% | **57.5%** | 55~60 | **○ 범위 안** |
| 지름 | 11.5 | 17.9 | **15.5** | 15~18 | **○ 범위 안** |
| 순환 계수 | 24.2 | 15.6 | 17.4 | 12~15 | △ 초과 |
| 막다른 방 | 15.2% | 15.3% | 15.2% | 14~22 | ○ |
| **민첩 0 탈출구 (V2)** | 100% | 100% | **100%** | 100 | ○ **모든 크기** |
| 생성 시간 | 7.26 ms | 7.07 ms | **7.2~8.1 ms** | 16.7 미만 | ○ |

**그림에서 네 시드 모두 같은 패턴이 나온다** (`.shots/dungeon_compare.html`, 방 50개).

| 시드 | 빠른 길 | 돌아가는 길 |
| --- | --- | --- |
| 11 | 4칸, 최대 상승 **4** | 7칸, 최대 상승 **2** |
| 23 | 5칸, 최대 상승 **3** | 12칸, 최대 상승 **2** |
| 47 | 6칸, 최대 상승 **3** | 8칸, 최대 상승 **2** |
| 91 | 5칸, 최대 상승 **4** | 7칸, 최대 상승 **2** |

**돌아가는 길의 최대 상승폭이 넷 다 정확히 2 다.** 우연이 아니라 구성이다 —
계단을 목표에서 거꾸로 놓고 한 칸에 `GENTLE_SLOPE` 이상 오르지 않게 묶는다.
평균값(+1.06)이 목표에 못 미치는 것은 **빠른 길의 상승폭이 판마다 3~4 로 흔들리기** 때문이고,
판 하나하나에서는 성격이 분명히 갈린다.

### 17.17.15 덩어리 4 에서 고친 것 — 여덟 번

**하나를 고치면 다음 것이 드러났다.** 전부 기존 테스트가 잡았다.

| # | 증상 | 원인 | 고침 |
| --- | --- | --- | --- |
| 1 | 테스트 46개 실패 | 새 `class_name` 이 전역 목록에 없다 | `--import` 를 먼저 돌린다 |
| 2 | 보스 필요 민첩 0 (V3) | 탈출 경로가 보상 방을 지나 봉우리가 깎였다 | 보상을 비켜 가는 탈출 경로를 찾는다 |
| 3 | 보스 필요 민첩 0 (V3) | 우회로가 없는 판에서 `sharpen` 이 그냥 물러났다 | 우회로가 없어도 봉우리는 세운다 |
| 4 | 막다른 방 4.8% (V5) | 노출·경로 시공이 V5 보정 **뒤에** 와서 정찰 지점을 먹었다 | **막다른 방 관리를 위상의 마지막으로** 옮겼다 |
| 5 | 생성 14.6 ms | `pick_prize` 가 후보마다 여유 검사를 다시 했다 | 한 번만 걸러 넘긴다 |
| 6 | 우회로가 지름길보다 가팔랐다(5 대 3) | 봉우리에 "이웃보다 1 은 높게" 를 함께 요구해 계단이 닿는 높이를 넘었다 | **경사로 보장이 봉우리 높이보다 앞선다** |
| 7 | 보스가 평범한 방보다 싸게 닿았다 | 경사로를 한 칸에 1 로 묶으니 봉우리가 우회로 길이에 갇혔다 | 완경사를 **기본 민첩(2)** 으로. 여전히 걸어서 오른다 |
| 8 | 생성 20~30 ms | 탈출구 후보마다 경로를 찾아 봤다 | **너비 우선 탐색을 한 번만** 한다 |

**4번과 6번이 이 덩어리에서 배운 것이다.**

> **4번 — 순서가 규칙을 이긴다.** V5 를 맞춰 놓고 그 뒤에 간선을 더하면 V5 는 이미 옛말이다.
> 보정은 **그 값을 바꿀 수 있는 모든 단계 뒤**에 와야 한다.
>
> **6번 — 두 보장이 부딪히면 어느 것이 앞서는지 정해야 한다.**
> 「봉우리는 이웃보다 높다」와 「우회로는 완만하다」가 동시에 성립하지 않는 판이 있었다.
> 둘 다 지키려다 **둘 다 깨졌다.** 사용자가 요구한 것은 두 경로이므로 그쪽을 앞세웠다.
> 그 판에서 보상 방이 이웃보다 낮을 수는 있다 — **적어 두고 넘어간다.**

### 17.17.16 남은 미달과 그 이유

| 항목 | 지금 | 목표 | 왜 남았나 |
| --- | --- | --- | --- |
| 보스 방 차수 | 3.67 | 4 이상 | `expose` 가 4 를 만들지만 여유를 지키는 들로네 후보가 모자란 판이 있다. 억지로 붙이면 통로가 방을 스친다 |
| 두 경로 상승폭 차 | +1.06 | 1.5 이상 | 빠른 길의 상승폭이 3~4 로 흔들린다. 판별로는 갈리지만 평균이 낮다 |
| 순환 계수 | 17.4 | 12~15 | 노출(간선 4개)과 시공한 두 번째 길이 간선을 더한다. `extra_edge_ratio` 를 낮춰 상쇄할 수 있지만 **차수 3 이상(57.5%)이 범위에 겨우 들어와 있어** 함께 내려간다 |

**셋 다 손잡이 하나를 당기면 다른 것이 밀리는 관계다.** 지금은 사용자 요구 셋 중
둘(대안 경로 100%, 귀중품 역할 분리)이 보장이고 하나(상승폭 차)가 판별 보장이다.

---

## 17.18 덩어리 5 — 던전마다 성격 (2026-08-08, 완료)

`src/core/dungeon/dungeon_catalog.gd` 가 §7.3.1 의 다섯을 들고 있다.

### 17.18.1 성격은 크기와 다른 축이다

게이트 등급이 이미 던전의 **크기**를 정한다(`GateRank.dungeon_size`).
성격이 방 개수까지 정하면 등급이 뜻을 잃는다. 그래서 카탈로그는 **성격 축만** 얹는다.

```
크기(게이트 등급) -> room_count
성격(카탈로그)    -> 나머지 축
```

`Gate.dungeon_character` 가 어느 성격인지 들고 다닌다. **아직 그것을 고르는 아웃게임 화면이
없어 기본값은 0 이다.** 개발 화면에서는 「새 판 찍기」를 누를 때마다 차례로 돈다.

성격은 **값을 치르고 얻는 정보**로 뒀다 — 게이트 미리보기의 소문 수준에서는 등급만 보이고,
답사 수준부터 성격 이름이 보인다. 소문 수준이 등급만 보여 준다는 계약은 `test_gate.gd` 가 지킨다.

### 17.18.2 다섯이 한 판만 봐도 갈리는가

**같은 시드 · 같은 방 개수(29)** 로 그린 판이다 (`.shots/dungeon_characters.html`).

| 성격 | 간선 / 고리 | 평지 | 최고 고도 | 탈출구 | 막다른 방 | 귀중 / 위험 |
| --- | --- | --- | --- | --- | --- | --- |
| 얕은 갱도 | 33 / **5** | 61% | **4** | 10칸 | 5 | 4 / 3 |
| 벌집 | **45 / 17** | **44%** | 7 | 6칸 | 4 | 4 / 3 |
| 수직 회랑 | 37 / 9 | 57% | **10** | 9칸 | 5 | 4 / 3 |
| 먼 출구 | 34 / 6 | 59% | 7 | 6칸 | **8** | 4 / 3 |
| 금고층 | 37 / 9 | 59% | 7 | 4칸 | 5 | **1 / 9** |

**다섯 다 머리글 한 줄로 갈린다.** 다만 그렇게 되기까지 두 번 손봤다.

**「먼 출구」는 축 하나로는 갈리지 않았다.** 탈출구까지의 거리만 흔들었더니
**성긴 판(얕은 갱도)이 우연히 더 먼 탈출구를 갖는 일이 흔했다** — 신호대잡음 1.68 로
쓰는 축 중 가장 약하다. 막다른 방 하한을 함께 올렸다. 둘은 **상쇄하지 않고 같은 방향으로
민다** — 막다른 방이 늘면 지름이 늘고 탈출구도 함께 멀어진다.

> **축을 하나 더 줄 때는 상쇄하지 않는 것을 골라야 한다.**
> §17.16.3 이 이미 재 뒀다 — 귀중품을 올리면 위험방이 줄고, 곁가지를 올리면 보스 필요
> 민첩이 내린다. 그런 짝을 한 던전에 같이 주면 서로 지워 「그냥 다른 판」이 된다.

**그리고 「먼 출구」의 이름이 실제보다 많이 약속하고 있었다.**
`exit_distance_ratio` 는 **그 판의 최대 깊이에 대한 비율**이라 "다른 던전보다 멀다" 를
뜻하지 않는다. 위 표에서도 얕은 갱도 10칸 대 먼 출구 6칸이다.
요약을 「판 반대편에 있다」에서 **「그 판의 가장 먼 끝에 있다」**로 고쳤다.

> **상대값 축은 이름이 절대값을 약속하면 안 된다.**

### 17.18.3 보장 — 성격마다, 크기마다 (24판씩)

| 크기 3 (칸 22개 안팎) | 간선 | 고리 | 막다른 방 | 지름 | 민첩 2 | 탈출홉 | 귀중 | 위험 | **V2** |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 얕은 갱도 | 26.5 | 6.3 | 17.1% | 8.0 | 100.0% | 5.5 | 2.8 | 2.1 | **100%** |
| 벌집 | 32.9 | 12.8 | 15.3% | 6.9 | 96.5% | 4.9 | 2.8 | 2.1 | **100%** |
| 수직 회랑 | 28.4 | 8.3 | 16.1% | 7.2 | **78.8%** | 5.2 | 2.8 | 2.1 | **100%** |
| 먼 출구 | 26.9 | 6.7 | **28.7%** | 8.0 | 97.1% | **6.5** | 2.8 | 2.3 | **100%** |
| 금고층 | 28.4 | 8.3 | 15.7% | 7.3 | 97.0% | 5.5 | **1.0** | **5.8** | **100%** |

**민첩 0 탈출구(V2)가 성격 다섯 x 크기 다섯 = 25 조합에서 전부 100% 다.**
나머지 보장(V1 · V3 · V5 · V7 · V8 · V9)은 테스트가 크기와 시드를 훑으며 지킨다.

생성 시간은 방 50개에서 **8.5 / 8.8 ms** (두 번 쟀다). 프레임 예산 16.7 ms 안이다.

### 17.18.4 규칙은 테스트가 지킨다

`test/unit/test_dungeon_catalog.gd` — **값이 아니라 규칙을 못 박는다.**
다섯의 이름과 수치는 기획이라 뒤집히지만 아래가 깨지면 성격이 성격으로 읽히지 않는다.

| 테스트 | 규칙 |
| --- | --- |
| 축이 둘 이하 | R1 — 셋 이상이면 어느 것이 성격인지 안 읽힌다 |
| 잡음 축을 쓰지 않는다 | R2 — 층 안 기복 · 기복의 잔 정도는 금지 |
| 방 개수를 건드리지 않는다 | 크기는 게이트 등급의 축이다 |
| 다섯이 실제로 다른 판을 만든다 | 같은 시드에서 같은 판이면 이름표일 뿐이다 |
| 같은 성격 · 같은 시드 -> 같은 판 | §17.7 재현성 |
| 색인이 범위 밖이어도 죽지 않는다 | 아웃게임이 아직 성격을 고르지 않는다 |

### 17.18.5 `wrap` 이라는 이름을 쓸 수 없었다

`DungeonCatalog.wrap(i)` 로 한정해 불러도 **GDScript 내장 `wrap(value, min, max)` 쪽으로
풀려** "인자가 모자라다" 로 죽었다. `wrapped()` 로 바꿨다.

> **한정해 불러도 내장 이름이 이긴다.** 짧고 흔한 이름을 static 메서드에 붙이지 않는다.

---

## 17.19 이 레인이 남기는 것 (2026-08-08)

설계안 다섯 덩어리를 모두 옮겼다. **남은 것은 아래가 전부다.**

### 17.19.1 목표에 못 미친 것

| 항목 | 지금 | 목표 | 무엇을 해야 하나 |
| --- | --- | --- | --- |
| 보스 방 차수 | 3.67 | 4 이상 | `expose` 가 여유를 지키는 들로네 후보를 못 찾는 판이 있다. 억지로 붙이면 통로가 방을 스친다 (§17.6). 방 자리를 뿌릴 때 보상 후보 주변을 더 촘촘하게 하는 쪽이 남았다 |
| 두 경로 상승폭 차 | +1.06 | 1.5 이상 | **돌아가는 길은 이미 항상 2 다**(구성으로 보장). 모자란 것은 빠른 길이 3~4 로 흔들리는 쪽이다. `SHORTCUT_CLIMB` 을 올리면 되지만 필요 민첩이 함께 올라 V3 와 부딪힌다 |
| 순환 계수 | 17.4 | 12~15 | 노출(간선 4개)과 시공한 두 번째 길이 더한 몫이다. `extra_edge_ratio` 를 낮춰 상쇄할 수 있지만 **차수 3 이상(57.5%)이 범위에 겨우 들어와 있어** 함께 내려간다 |
| 갈림길 | 57.5% | 55~60 | **범위 안이다.** 다만 위 순환 계수와 같은 손잡이라 하나를 고치면 이쪽이 밀린다 |

**셋은 손잡이 하나를 당기면 다른 것이 밀리는 관계다.** 따로 고칠 수 없다.

### 17.19.2 확인하지 못한 것

| 항목 | 상태 |
| --- | --- |
| **웹 빌드** | **미확인.** 제출물이 웹이고 브라우저 캔버스 크기가 다르면 화면 결론이 달라진다. 이동 레인이 웹 실측 절차를 `src/proto/unit_move/README.md` 에 적어 뒀다 — **웹은 `-s` 스크립트를 못 돌려 재려는 것을 장면으로 만들어야 한다** |
| 방 64개 화면 | 확대 하한에서 91% 만 보인다. 요청은 50개라 그대로 뒀다. 64까지 담으려면 간격을 220 까지 내려야 하고 그때 최소 간격이 44 px 로 준다 (§17.15.6) |
| 방 50개에서 추리가 성립하는가 | **미확인.** 정보량이 방 수에 비례해 늘지 않으므로(§17.2) 판이 커지면 추리가 노동이 될 수 있다. **굴려 봐야 안다** |
| 낮은 배율에서 방 종류 구분 | UI 킷에 규격이 나왔고(쐐기 · 빗금 · 결손) **우리 여섯 종류와 맞는다.** 아직 적용하지 않았다 |

### 17.19.3 다음에 이어받을 사람에게

**새로 만들 것으로 남겨 둔 것 셋.**

| 항목 | 왜 |
| --- | --- |
| **탈출구 개수 파라미터** | `07` §7.6 이 "개수와 위치가 「언제 나갈까」의 무게를 결정한다" 고 적었는데 지금은 **언제나 하나**다. 개수는 시드에 흔들리지 않아 성격 축으로 좋다 |
| **보스가 치르는 값 선택** | §17.4.1 이 두 축(보스=민첩, 탈출구=거리)을 정의했는데 이 배정이 고정이다. 「가깝지만 못 올라간다」 대 「능력은 되는데 멀다」가 던전 성격이 될 수 있다 |
| **물량전이 일어나는 방** | 전투가 확정되면서 물량전과 보스전이 방 안에서 일어난다. 지금 방 종류에 그 자리가 없다 |

**그리고 이 레인이 배운 것 중 다음에도 걸릴 것.**

| 교훈 | 어디서 나왔나 |
| --- | --- |
| **재지 않으면 헛돈다** | 통로가 방을 스치는 원인을 두 번 추측으로 틀리고, 찍어 보는 스크립트를 돌리자 바로 나왔다 (§17.17.9) |
| **최선이 나쁘면 결과가 나쁘다** | 고를 때 물러나지 말고 **후보에서 빼라.** 관문 · 보상 자리 · 탈출구에서 세 번 걸렸다 |
| **순서가 규칙을 이긴다** | 보정은 그 값을 바꿀 수 있는 **모든 단계 뒤**에 와야 한다 (§17.17.15) |
| **두 보장이 부딪히면 우선순위를 정해라** | 둘 다 지키려다 둘 다 깨졌다 (§17.17.15) |
| **구조를 바꾸면 성격 축을 다시 재라** | 신호대잡음 표만 세 판이다. 축 하나가 죽고 하나가 살아났다 (§17.16.9 · §17.16.10) |
| **뜻이 바뀐 파라미터는 값을 쓰는 곳을 전부 다시 봐라** | `elevation_gain` 이 두 번 바뀌었다 (§17.17.12) |
| **시간은 한 번 재지 마라** | 8.66 과 7.07 이 나왔고 잡음이었다 (§17.17.11) |
| **축을 살리는 방법은 둘이다** | 시드 폭을 줄이거나 값을 더 벌리거나 (§17.16.10) |

### 17.17.10 덩어리 3 — 고도를 구역 등급에서

**바뀐 것**

| 무엇 | 예전 | 지금 |
| --- | --- | --- |
| 고도 | 입구에서의 거리 x 0.85 | **구역 등급 x 2** |
| 층 안 기복 | 진폭 2.6 | 진폭 1.0 (단차보다 작아야 층이 읽힌다) |
| 기복 주파수 | 0.16 | 0.22 |
| `elevation_gain` 의 뜻 | 방 하나 거리당 상승 | **구역 등급 하나당 단차** |

**단차 2 가 기본인 것이 이 덩어리의 판단이다.** 스쿼드 기본 민첩이 2 이므로
**관문은 걸어서 넘을 수 있다.** 프로토타입에서 단차 3 으로 두었더니 갈 수 있는 방이
20% 까지 붕괴했고, 고도 폭을 고정해 구역 수로 나누니 작은 판만 가팔라졌다.
**지형으로 판 전체를 막지 않는다 — 벽은 봉우리와 지름길에만** (덩어리 4).

### 17.17.11 실측 — 덩어리 3 (방 50개, 40판)

| 항목 | 덩어리 2 | **덩어리 3** | 뜻 |
| --- | --- | --- | --- |
| 순환 계수 · 차수 · 지름 | 15.6 · 2.63 · 17.7 | 15.6 · 2.63 · **17.9** | 위상은 그대로다 (고도는 점수에만 쓰인다) |
| 민첩 2 로 열리는 방 | 70.3% | **73.7%** | 지형이 걸어 다닐 수 있다 |
| **민첩 0 탈출구 (V2)** | 100% | **100%** | **모든 크기에서** |
| 보스 필요 민첩 | — | 5.9 | 봉우리가 없으니 아직 지형에서 나온 값이다 |
| 생성 시간 | 7.53 ms | **7.07 ms** | 프레임 예산(16.7) 안. 아래 주석 참고 |

크기별 V2 와 열리는 방 비율.

| 목표 방 | 12 | 22 | 32 | 50 | 64 |
| --- | --- | --- | --- | --- | --- |
| 민첩 0 탈출구 | 100% | 100% | 100% | 100% | 100% |
| 민첩 2 로 열리는 방 | 97.3% | 95.5% | 82.9% | 73.7% | 81.2% |

**작은 판이 가팔라지는 회귀는 없다.** 프로토타입에서 방 12개 V2 가 92.5% 로 떨어졌던
전례가 있어 크기마다 따로 쟀다.

**생성 시간을 두 번 재서 8.66 ms 와 7.07 ms 가 나왔다. 잡음이었다.**
처음 값을 보고 "고도 계산은 방 개수에 선형인데 왜 늘었나" 를 의심해 다시 쟀고,
다른 레인이 동시에 Godot 을 돌리는 동안 잰 값이 15% 부풀려져 있었다.
**7.07 ms 가 맞고 덩어리 3 은 생성을 느리게 하지 않았다.**

> **한 번 잰 수를 그대로 적으면 잡음을 사실로 기록한다.**
> 이 저장소에서 다른 레인들이 Godot 을 동시에 돌리므로 시간 측정은 특히 그렇다.
> **의심스러운 수는 다시 재고, 다시 재기 전에는 「확인 안 됨」이라고 적는다.**

### 17.17.12 덩어리 3 이 밝힌 것 — 축 하나가 탈락했다

**구조를 바꾸면 성격 축의 값어치도 바뀐다.** 덩어리 2~3 을 적용하고 §17.16 을 다시 재니
**탈출구 최소 거리의 신호대잡음이 1.9 에서 0.75 로 떨어졌다.**

축을 흔든 폭은 4.2 -> 5.2 홉으로 늘었는데 **시드 폭이 2.2 -> 6.9 홉으로 세 배**가 됐다.
지름이 11.5 -> 17.9 로 늘어난 대가다 — 판이 길어지면 탈출구까지의 거리가 시드에 따라
크게 흔들린다.

> **축을 흔든 폭만 보면 그 축은 더 세졌다.** 시드 폭과 견주지 않으면
> **탈락한 축을 계속 성격으로 쓰게 된다.**

`07` §7.3.1 의 「먼 출구」를 **보류로 내렸다.** 되살리려면 거리가 아니라 **탈출구 개수**를
흔들어야 한다 — 개수는 시드에 흔들리지 않는다.

그리고 **「수직 회랑」의 값을 고쳤다.** `elevation_gain` 1.60 은 옛 뜻(거리당 상승)에서
가파른 값이었지만 새 뜻(등급당 단차)에서는 기본값 2.0 보다 **작아서 오히려 평평해진다.**
4.0 으로 올렸다 — 민첩 2 로 열리는 방이 80.9% 에서 30.8% 로 떨어진다.
**뜻이 바뀐 파라미터를 값만 두고 넘기면 의도가 뒤집힌다.**

### 17.17.4 덩어리 1 에서 테스트가 잡은 것

**각도 순 고리만으로는 순환이 보장되지 않는다.** 구역 8개짜리 판에서 연결이 7개,
즉 **나무**가 나왔다. 각도 순으로 이웃이 된 두 구역이 들로네에서 맞닿아 있지 않으면
그 연결을 건너뛰는데, 그러면 고리가 닫히지 않고 연결성 채우기가 나무로 마감한다.

**프로토타입에서는 이게 드러나지 않았다.** 덩어리 4 의 경로 시공(`_ensure_two_routes`)이
없는 간선을 파서라도 두 경로를 만들기 때문에 보스 대안 경로가 100% 로 나왔고,
**고리가 없는 판이 섞여 있다는 사실이 그 수치에 가려졌다.**
지표가 좋아 보이는 것이 구조가 옳다는 뜻은 아니었다.

그래서 `ring()` 에 3단계를 넣었다 — **입구 구역이 순환 위에 없으면 순환을 만드는 쌍을
하나 더 잇는다.** 어떤 쌍을 이어야 하는가는 이렇게 정한다.

> **입구를 지웠을 때 서로 끊어지는 두 구역**을 잇는다.
> 그 둘을 이으면 원래 입구를 거쳐 가던 길에 우회로가 생기므로 입구가 순환 위에 놓인다.

입구 구역이 맞닿은 구역이 하나뿐이면 순환을 만들 수 없다. 그때는 연결만 보장하고 물러난다 —
두 경로는 덩어리 4 가 구역 **안**에서 만든다.

**테스트도 함께 강화했다.** 처음에 쓴 「입구 구역의 등급이 0」은 `ranks()` 가 시작 구역을
0 으로 두므로 **아무것도 확인하지 않는 테스트**였다. 지금은 「입구가 아닌 구역은 전부 1 이상」과
「관문을 넘을 때 등급 차가 1 이하」를 함께 본다.

### 17.17.5 덩어리 1 이 못 박은 성질

`test/unit/test_dungeon_zones.gd` — 11개 테스트.

| 성질 | 왜 필요한가 |
| --- | --- |
| 모든 방이 정확히 한 구역에 속한다 | 빠진 방은 어느 관문에도 안 걸린다 |
| 빈 구역이 없다 | 빈 구역의 관문과 등급은 허수다 |
| 입구 구역만 등급 0 이고 나머지는 1 이상 | 고도가 등급에서 나오므로 입구가 판의 바닥이어야 한다 (§17.3.6) |
| 관문을 넘을 때 등급 차가 1 이하 | 아니면 BFS 가 아니라 다른 것이 된다 |
| 고리가 모든 구역을 잇는다 | 갈라진 판이 나오면 **V1** 이 깨진다 |
| **입구 구역이 순환 위에 있다** | 순환 밖이면 두 경로가 앞부분을 공유해 한 길이 된다 |
| 경계에는 구역을 넘는 간선만 담긴다 | 같은 구역 간선이 관문 후보가 되면 안 된다 |
| **관문이 들로네 간선이다** | 아니면 교차가 되살아난다 (**V8**) |
| 관문이 그 경계에서 가장 짧다 | 긴 관문은 판을 가로질러 읽히지 않는다 (§17.6) |
| 같은 입력 -> 같은 구역 | 시드 고정이 깨지면 버그를 재현할 수 없다 (§17.7) |
| 구역 수가 3~8 안에 있다 | 둘 아래는 고리가 없고, 여덟 위는 구역 안에 갈림길이 없다 |
| 방이 구역 수보다 적어도 죽지 않는다 | 아주 작은 판의 계약 |

### 17.17.6 덩어리 2 — 간선 선택이 구역을 쓴다

**바뀐 것**

| 무엇 | 어떻게 |
| --- | --- |
| 뼈대 | 판 전체 RNG∪MST -> **구역 안** RNG |
| 구역 사이 | 없었다 -> **관문 하나씩** (`DungeonZones.gate`) |
| 곁가지 | 가브리엘 전체 -> **허브에 닿는 것 우선** (구역당 허브 2개) |
| 점수 | 완만함 x **얕음** x 흔들림 -> 완만함 x 흔들림 |
| 연결성 | MST 가 보루 -> **수선**이 마감 (`_repair`) |

**「얕음」 항을 뺀 근거**는 §17.11.7-2 다. 깊은 곳을 성기게 만들려던 항이
얕은 쪽 차수 3.4 대 깊은 쪽 2.05 를 만들어 **깊은 절반을 사실상 나무**로 바꿨고,
하필 그 깊은 곳에 보상이 놓였다. 그 일은 이제 구역과 관문이 대신한다.

**MST 를 뼈대에서 뺀 근거**는 후보 집합이 줄었기 때문이다. 판 전체 들로네를 후보로 주면
Kruskal 이 짧은 간선만 고르지만, 후보를 구역 안으로 줄이면 구역이 기하적으로 두 조각일 때
그 둘을 잇는 **긴** 간선을 받아들인다. 연결성은 `_repair` 가 더 안전하게 마감한다.

### 17.17.7 실측 — 목표에 얼마나 왔나 (방 50개, 40판)

| 항목 | 옛 생성기 | **덩어리 2** | 목표 | 판정 |
| --- | --- | --- | --- | --- |
| 간선 | 69.4 | 60.8 | — | |
| 순환 계수 | 24.2 | **15.6** | 12~15 | ○ 근접 |
| 평균 차수 | 3.00 | **2.63** | 2.5~2.6 | ○ |
| 차수 1 | 15.2% | 15.4% | 14~22 | ○ |
| 차수 3 이상 | 69.1% | **54.2%** | 55~60 | △ 0.8%p 미달 |
| **지름** | 11.5 | **17.7** | 15~18 | **○ 달성** |
| 민첩 2 로 열리는 방 | 77.4% | 70.3% | — | |
| 생성 시간 | 7.26 ms | 7.53 ms | 16.7 미만 | ○ |

**지름 17.7 이 이 덩어리의 성과다.** 목표 범위(15~18)에 들어왔고, 「길을 찾는 재미」의
직접 지표다(§17.12.6). 방 개수를 늘리지 않고 **관문을 지나게 만든 것**으로 얻었다.

**차수 3 이상이 54.2% 로 0.8%p 모자란다.** 프로토타입이 50.4% 였으니 개선됐지만
목표에는 아직이다. 허브 셋으로 늘리면 순환 계수가 목표를 벗어난다(§17.14.3의 같은 손잡이).

아래는 아직 덩어리 3~4 의 몫이라 지금 값이 목표가 아니다.

| 항목 | 덩어리 2 | 어디서 해결되나 |
| --- | --- | --- |
| 보스 대안 경로 20.0% | 아직 우연이다 | 덩어리 4 (경로 시공) |
| 두 경로 상승폭 차 -0.09 | 고도가 아직 거리 원뿔이다 | 덩어리 3~4 |
| 보스 방 차수 1.60 | 아직 배치가 마지막 단계다 | 덩어리 4 |

### 17.17.8 보장 확인 — 깨진 것은 없다

| 보장 | 확인 방법 | 결과 |
| --- | --- | --- |
| **V1** 도달 | `test_dungeon_generator.gd` · `_repair` | 통과 |
| **V2** 민첩 0 탈출구 | 계측(`exitV2`)에서 **모든 크기 100%** | 통과 |
| V3 고가치 민첩 | `test_dungeon_generator.gd` | 통과 |
| **V5** 막다른 방 14~30% | 계측 15.2~17.0% | 통과 |
| **V7** 입구 차수 | `test_dungeon_generator.gd` | 통과 |
| **V8** 교차 없음 | `test_dungeon_layout.gd` | 통과 |
| V9 밀도 | 손대지 않았다 | 통과 |

### 17.17.9 덩어리 2 에서 테스트가 잡은 것 — 통로가 방을 스친다

**세 번 고쳤고 세 번 다 원인이 달랐다.** 깨진 것은 V1~V9 가 아니라 §17.6 의 품질 기준
「선이 상관없는 방을 관통하지 않는다」였다. 기준은 방 간격의 0.3 배(84 px)인데
통로가 방 **17~24 px** 앞까지 다가왔다.

| # | 원인 | 고친 방법 |
| --- | --- | --- |
| 1 | 구역 안 MST 가 구역을 잇느라 **긴 간선**을 받았다 | 뼈대에서 MST 를 빼고 `_repair` 로 넘겼다 |
| 2 | 곁가지 3겹이 「나머지 들로네」였다. 1·2 겹이 작아 3겹까지 내려가는 판이 흔했다 | 3겹을 **구역을 넘는 가브리엘**, 4겹을 **읽히는 것만**으로 나눴다 |
| 3 | 관문을 최단 간선으로 골랐다. 여유가 큰 것으로 물러나게 해도 남았다 — **그 구역 쌍의 모든 경계 간선이 방을 스쳤다**(최대 여유 0.11, 기준 0.35) | **고를 때 물러나지 말고 후보에서 뺀다** (`readable_boundaries`) |

**3번이 이 덩어리에서 배운 것이다.**

> **고를 수 있는 것 중 최선을 고르는 것으로는 모자라다. 최선이 나쁘면 결과가 나쁘다.**
> 후보를 걸러야 할 때 고르는 쪽을 손보면 「가장 덜 나쁜 것」이 통과한다.

그리기 어려운 경계를 가진 구역 쌍은 **이어질 수 없는 쌍**이 되고 고리가 그 쌍을 우회한다.
구역 그래프가 갈라지더라도 **방 단위 연결은 `_repair` 가 보장한다** (V1).

**측정이 아니라 추측으로 두 번 헛돌았다.** 원인을 찍어 보는 조사 스크립트를 먼저 돌렸을 때
바로 나왔다 — 문제의 간선이 **구역을 넘고, 가브리엘이 아니고, 그 쌍의 관문이었다.**
다음에는 첫 실패에서 바로 찍어 본다.

---

## 17.20 던전을 고르는 수치 — 규모 · 복잡 · 험난 (2026-08-08, 배선됨)

> 사용자 요구: **"얼마나 큰지, 얼마나 복잡한지, 얼마나 고도차가 심한지"** 를 보고 고른다.
> 축 이름은 **규모 · 복잡 · 험난**. 셋 다 **구조**의 성질이고, 보상·위험방 같은
> **내용물은 이번 범위가 아니다.**
>
> **아직 배선하지 않았다.** 축이 틀리면 배선이 통째로 낭비라 재고 먼저 보고한다.
> 계측 도구는 `tools/survey_dungeon_dials.gd`.

### 17.20.1 「축 둘까지」와 부딪히는 문제

§17.16.4 R1 은 **한 던전에 축 둘까지**로 제한했다. 축이 서로 상쇄하기 때문이다.
그런데 「복합적으로」는 셋을 동시에 요구한다.

**푸는 방법은 서로 독립인 축을 고르는 것이다.** 상쇄하는 짝은 축이 둘이 아니라
한 축의 양면이므로 하나로 합친다. 그래서 이 절은 **무엇을 축으로 삼을지가 아니라
무엇이 실제로 독립인지**를 잰 기록이다.

### 17.20.2 표시 지표를 고른 근거 — 어느 입력에 반응하는가

입력 격자(방 12~50 x 곁가지 0.08~0.46 x 단차 1~5)에 시드 12개씩. 값은 신호대잡음이다.

| 표시 후보 | 규모에 반응 | 곁가지에 반응 | 단차에 반응 | 시드 편차 | 판정 |
| --- | --- | --- | --- | --- | --- |
| **방 개수** | **11.30** | 0.00 | 0.00 | 1.54 | **규모** |
| 평균 차수 | 0.08 | **2.76** | 0.09 | 0.11 | **복잡의 등급 기준** |
| 갈림길 비율 | 0.25 | 1.77 | 0.08 | 6.59 | **복잡이 뜻하는 것** |
| 순환 계수 | 4.32 | 2.98 | 0.08 | 1.54 | x 규모와 섞인다 |
| 순환 밀도 | 0.45 | 2.75 | 0.09 | 0.06 | 평균 차수와 같은 것 |
| 지름 | 3.94 | 1.08 | 0.04 | 1.15 | x **규모가 말해 준다** |
| 막다른 방 비율 | 0.26 | 0.25 | 0.08 | 1.72 | x **아무것에도 반응 안 한다** |
| 최고 고도 | 2.41 | 0.04 | 2.25 | 1.65 | x 규모와 섞인다 |
| 못 가는 방 비율 | 0.67 | 0.11 | 1.61 | 9.76 | x 편차가 크다 |
| **간선당 평균 상승폭** | 1.02 | 0.26 | **1.89** | 0.37 | **험난** |
| 상승 3+ 간선 비율 | 0.58 | 0.17 | 1.48 | 7.24 | x 편차가 크다 |
| 상승 있는 간선 비율 | — | — | — | 13~24 | x **단조도 아니다** |

**세 가지가 이 표에서 결정됐다.**

- **지름은 축이 아니다.** 규모에 3.94, 곁가지에 1.08 로 반응한다 — **「판이 길다」는 규모가 말한다.**
- **막다른 방 비율은 축이 될 수 없다.** 어느 입력에도 반응하지 않는다(0.25~0.26).
  V5 보정이 14~30% 로 묶어 두기 때문이다. **이미 상수인 것을 표시해 봐야 뜻이 없다.**
- **최고 고도를 험난으로 쓰면 안 된다.** 단차(2.25)만큼 **규모(2.41)에도 반응한다** —
  판이 크면 구역이 늘어 등급이 높아지고 최고 고도가 따라 오른다.
  **높은 것과 험한 것은 다르다.** 험난은 **한 걸음에 얼마나 오르느냐**다.

### 17.20.3 셋이 독립인가 — 상관

| | 규모(방 개수) | 복잡(갈림길) | 험난(평균 상승폭) |
| --- | --- | --- | --- |
| **규모** | 1.00 | **0.09** | 0.37 |
| **복잡** | 0.09 | 1.00 | **0.13** |
| **험난** | 0.37 | 0.13 | 1.00 |

**규모 <-> 복잡 0.09, 복잡 <-> 험난 0.13.** 사실상 독립이다.
규모 <-> 험난 0.37 만 약한 상관이 남는다 — 큰 판일수록 구역이 늘어 층이 하나 더 생기는 몫이다.

**그림에서도 그대로 보인다** (`.shots/dungeon_dials.html`, 같은 시드 · 같은 방 개수).

| 입력 | 갈림길 | 평균 상승폭 |
| --- | --- | --- |
| 복잡 0.06 · 험난 0.5 | **31%** | 0.50 |
| 복잡 0.06 · 험난 5.0 | **31%** | **1.81** |
| 복잡 0.24 · 험난 0.5 | **55%** | 0.51 |
| 복잡 0.24 · 험난 5.0 | **55%** | **2.73** |
| 복잡 0.60 · 험난 0.5 | **72%** | 0.66 |
| 복잡 0.60 · 험난 5.0 | **72%** | **2.00** |

**험난을 열 배로 올려도 갈림길 비율이 소수점까지 같다.** 독립이 계산이 아니라 그림에서 확인된다.

### 17.20.4 등급은 몇 단계까지 낼 수 있나 — **셋이 다르다**

각 단계의 평균에 ±2 표준편차 띠를 붙여 이웃과 겹치는지 봤다 (시드 12개).

| 축 | 표시 지표 | 다섯 단계 중 겹치는 이웃 | **낼 수 있는 등급** |
| --- | --- | --- | --- |
| 규모 | 방 개수 | **0 / 4** | **5단계** |
| 복잡 | 평균 차수 | 2 / 4 | **3단계** |
| 복잡 | 갈림길 비율 | **4 / 4** | **등급으로 못 쓴다** |
| 험난 | 간선당 평균 상승폭 | 3 / 4 | **3단계** |

**「복잡 5단계」는 낼 수 없다.** 갈림길 비율은 다섯 단계가 **전부** 이웃과 겹친다
(0.02 -> 41.9±12.9, 0.14 -> 48.4±14.3 …). 시드가 그만큼 흔든다.

> **그래서 등급은 평균 차수로 매기고, 화면에는 「갈림길」로 설명한다.**
> 둘의 상관이 **0.97** 이라 같은 것을 말하는데, **평균 차수가 훨씬 덜 흔들린다**
> (편차 0.11 대 6.59). **읽히는 말과 재는 값이 다를 수 있다.**

**험난도 3단계다.** 상승폭이 커질수록 편차도 커진다(±0.24 -> ±1.73).

> **다섯 단계로 통일하지 않는다.** 규모만 5단계이고 복잡·험난은 3단계다.
> 안 갈리는 등급을 다섯 개 내놓으면 **화면이 거짓말한다.**

### 17.20.5 표시는 입력이 아니라 결과다 — 그리고 그것이 정보 설계와 맞는다

**입력이 5 인데 결과가 3 이면 화면이 거짓말한다.** 그래서 표시값은 전부
**생성된 판을 재서 나온 값**이다. 그리고 이것이 이미 정해진 규칙과 맞아떨어진다.

`22-guild-base.md` 가 **"해부 수준일 때만 설계도를 실제로 생성한다"** 고 못 박았다.
**재려면 설계도가 있어야 하므로, 잰 값은 해부 수준에서만 나올 수 있다.** 구현 제약과
정보 설계가 같은 곳을 가리킨다.

| 수준 | 지금 보이는 것 | **더할 것** | 성질 |
| --- | --- | --- | --- |
| 소문 | 이름 · 등급 | — | 등급이 규모를 **함축**한다 |
| 정찰 | + 대략적인 방 개수 | — | 입력에서 나온 **예상**이다 |
| 해부 | + 실제 방 개수 · 고가치 방 수 · 보스 유무 | **복잡 · 험난 등급** | 설계도를 재서 나온 **실제** |

**규모가 가장 싸고 복잡·험난이 비싸다.** §5.7 이 "정보가 이 게임의 주 자원" 이라고
확정했으므로 전부 공짜로 보이면 정보실을 키울 이유가 없어진다.

**그리고 예상과 실제가 크게 어긋나면 그 자체가 버그 신호다** — 계기가 하나 공짜로 생긴다.

### 17.20.6 지도를 일부만 공개하게 되면 (기록)

사용자가 나중을 위해 남긴 방향이다. **지금 만들지 않는다.** 다만 지금 정하는 수치가
어느 쪽인지는 갈라 둔다.

지금 `dungeon_board.gd` 는 **구조는 다 보여 주고 내용만 가린다** — 방과 통로는 전부 그리고
플레이어가 선 방 외에는 위험도를 `?` 로 둔다. 사용자가 말한 「일부공개」가 **구조까지
가리는 것**이면 그건 다른 화면이다.

| 수치가 사는 곳 | 무엇의 성질인가 | 지도가 가려지면 |
| --- | --- | --- |
| **게이트 목록** (이번에 정한 셋) | **그 던전 전체.** 정찰로 산 정보다 | **그대로 유효하다.** 오히려 무거워진다 — 들어가기 전에 아는 유일한 단서가 된다 |
| 지도 화면 | 지금까지 본 것 | 탐색하며 갱신되는 다른 수치가 필요해진다 |

**이번에 정한 셋은 전부 「지도를 보기 전에 아는 것」이다.** 지도가 가려져도 정의가 바뀌지 않는다.

### 17.20.7 카탈로그 다섯은 프리셋으로 남긴다

수치 축으로 가면 조합이 전부 표현되므로 이름이 필요 없어 보이지만, **남기는 쪽이 낫다.**

| 남기는 이유 | |
| --- | --- |
| 게시판을 훑는 속도 | 「복잡 3 · 험난 1」보다 「벌집」이 빠르게 읽힌다 |
| 축이 못 담는 것이 있다 | 「금고층」은 귀중품·위험방 비율을 흔든다. **그건 내용물이라 구조 축 셋에 안 들어간다** |
| 구조는 이름을 모른다 | 카탈로그는 축 값의 묶음일 뿐이라 이름이 바뀌어도 생성기는 영향이 없다 |

**단 이름과 수치가 어긋나면 안 된다.** 「벌집」인데 복잡 등급이 낮으면 거짓말이다.
배선할 때 **각 프리셋의 등급을 재서 이름과 맞는지 확인**해야 한다 — 아직 안 했다.

### 17.20.8 등급 경계 — 가운데가 가장 흔하게

실제로 쓰일 범위(게이트 등급 다섯 x 프리셋 다섯 x 시드 10개)의 분포를 보고 잘랐다.
**극단이 흔하면 등급이 뜻을 잃으므로 가운데 등급이 가장 넓다.**

| 축 | 분포 (25 / 50 / 75 백분위) | 경계 | 결과 |
| --- | --- | --- | --- |
| 규모 | 16 / 22 / 27 방 | 15 · 20 · 25 · 30 | 게이트 등급 다섯이 내는 방 개수의 사이를 자른다 |
| 복잡 | 2.55 / 2.67 / 2.85 차수 | **2.55 · 2.90** | 1 이 약 25%, 2 가 약 55%, 3 이 약 20% |
| 험난 | 0.74 / 1.06 / 1.32 상승 | **0.75 · 1.40** | 1 이 약 25%, 2 가 약 50%, 3 이 약 25% |

`DungeonGrade` 가 이 경계를 들고 있고, **설계도만 받는다 — 파라미터를 보지 않는다.**
손으로 짠 설계도에도 등급이 매겨진다(테스트가 그것을 확인한다).

### 17.20.9 프리셋 다섯의 등급 — 이름과 맞는가

크기 1 / 3 / 5 에서 시드 12개씩. **이름이 약속하는 축의 등급을 굵게 표시한다.**

| 프리셋 | 복잡 | 험난 | 이름이 약속하는 것 | 판정 |
| --- | --- | --- | --- | --- |
| 얕은 갱도 | **1.3** | **1.2~1.7** | 성기고 평평하다 | ○ |
| 벌집 | **3.0** | 2.0~2.2 | 촘촘하다 | ○ |
| 수직 회랑 | 1.8~2.1 | **2.5~3.0** | 고도가 심하다 | ○ |
| 먼 출구 | 1.5~1.8 | 1.6~2.0 | **탈출구가 멀다** (구조 축 아님) | ○ 해당 없음 |
| 금고층 | 1.8~2.1 | 1.8~2.0 | **귀중품이 적고 위험하다** (내용물) | ○ 해당 없음 |

**어긋나는 것이 없어 프리셋 값을 고치지 않았다.**
「먼 출구」와 「금고층」이 가운데 등급인 것은 **거짓말이 아니다** — 그 이름은 구조 축을
약속하지 않는다. 탈출구 거리와 내용물은 축 셋 밖이고, 그래서 프리셋을 남긴 것이다(§17.20.7).

### 17.20.10 규모 <-> 험난 0.37 을 받아들인다

**방이 많으면 구역이 늘고 층이 하나 더 쌓인다. 그건 이상하지 않다.**
다만 큰 판이 늘 험하면 험난 축이 반쯤 죽으므로 확인했다.

> **규모 4~5 인 판 100 개 중 험난 1 이 21 개.** 큰 판에서도 평평한 판이 나온다.

### 17.20.11 그림 — 등급이 눈으로 갈리는가

`.shots/dungeon_dials.html` — 같은 시드 · 같은 방 개수로 복잡 x 험난 격자.

| 입력 | 등급 | 갈림길 | 평균 상승폭 |
| --- | --- | --- | --- |
| 복잡 0.06 · 험난 0.5 | 복잡 **1** · 험난 **1** | 31% | 0.50 |
| 복잡 0.06 · 험난 5.0 | 복잡 **1** · 험난 **3** | 31% | 1.81 |
| 복잡 0.24 · 험난 0.5 | 복잡 **2** · 험난 **1** | 55% | 0.51 |
| 복잡 0.24 · 험난 5.0 | 복잡 **2** · 험난 **3** | 55% | 2.73 |
| 복잡 0.60 · 험난 0.5 | 복잡 **3** · 험난 **1** | 72% | 0.66 |
| 복잡 0.60 · 험난 5.0 | 복잡 **3** · 험난 **3** | 72% | 2.00 |

**험난을 1 에서 3 으로 올려도 복잡 등급과 갈림길 비율이 그대로다.** 그 반대도 같다.
독립이 계산이 아니라 그림에서 확인된다.

### 17.20.12 UI 킷에 넘기는 규격

**무엇을 보여야 하는지만 정한다. 어떻게 놓을지는 UI 킷이 정한다.**

| 수치 | 단계 | 어느 수준에서 | 화면에 쓸 말 |
| --- | --- | --- | --- |
| **규모** | **1~5** | 정찰(예상) · 해부(실제) | 「방이 몇 개인가」 |
| **복잡** | **1~3** | 해부 | 「갈림길이 얼마나 많은가」 |
| **험난** | **1~3** | 해부 | 「한 걸음에 얼마나 오르는가」 |

**단계 수가 축마다 다르다는 것이 규격의 핵심이다.** 셋을 같은 모양의 눈금으로 그리면
복잡·험난이 5단계인 것처럼 보이고, 그러면 화면이 거짓말한다.

근거 한 줄도 함께 준다 — **「갈림길 55% · 한 걸음 평균 상승 2.7」.**
등급만 보여 주면 왜 그 등급인지 알 수 없다.

**아직 안 정한 것:** 눈금의 생김새, 세 수치의 배치 순서, 프리셋 이름을 함께 보일지.

---

---

## 17.21 웹 실측 시도와 네이티브 경고 (2026-08-09)

### 17.21.1 네이티브에서 이미 예산을 넘는다

`tools/web_dungeon_bench.tscn` — **장면**으로 만들었다. 내보낸 웹 빌드는 `run/main_scene`
하나만 띄우고 `-s` 로 스크립트를 못 돌리기 때문이다(이동 레인이 찾은 방법,
`src/proto/unit_move/README.md` §22). **네이티브에서도 같은 장면을 돌려야 배수가 뜻을 가진다.**

한 프레임에 한 판씩, 예열 20 판 뒤 120 판.

| 조합 | 생성 중앙 | 생성 95 | 생성 최악 | 프레임 중앙 | 프레임 95 | 넘친 프레임 | 그중 우리 탓 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 규모3 • 복잡2 • 험난2 | 6.24 | 8.63 | 10.95 | 7.28 | 12.09 | 0 / 120 | 0 |
| **규모5 • 복잡3 • 험난3** | **12.83** | **14.98** | **17.99** | 13.93 | 17.09 | **7 / 120** | **7** |

**가장 무거운 조합은 네이티브에서 이미 프레임을 넘긴다.** 120 프레임 중 7 이 넘쳤고
**전부 우리 계산 탓**이다(그 프레임에서 생성이 예산의 절반을 넘게 썼다).

**웹은 여기서 더 나빠진다.** 이동 레인 실측에서 네이티브 대비 배수가 백분위를 따라
1.00 -> 1.38 -> 2.21 -> 3.76 으로 올랐다. **95 분위 14.98 ms 에 2 배만 곱해도 예산 밖이다.**
다만 이건 **추론이지 실측이 아니다.**

### 17.21.2 헤드리스 측정이 낙관적이었다

같은 조합을 헤드리스로 재면 8.4~9.8 ms 인데 장면에서는 12.83 ms 다.
**렌더와 같은 프레임에서 돌 때 더 든다.** §17.9 부터 이 문서의 시간은 전부 헤드리스였다 —
**그 값들은 실제보다 낙관적이다.**

축이 시간에 미치는 몫은 작다(헤드리스 기준 곁가지 +0.5, 단차 +0.5, 둘 다 +1.0 ms).
**무거운 것은 축이 아니라 방 개수와 렌더 동시성이다.**

> **주의:** 이 시간들은 다른 레인이 동시에 Godot 을 돌리는 중에 쟀다. 절대값은 흔들릴 수
> 있다(같은 판을 두 번 재서 8.66 과 7.07 이 나온 전례가 있다). **조합 사이의 비교는 유효하지만
> 절대값은 조용한 기계에서 다시 재야 한다.**

### 17.21.3 웹 실측은 막혔다 — 무엇이 준비됐고 무엇이 남았나

**빌드까지는 됐다.** `run/main_scene` 을 잠깐 벤치 장면으로 돌려 내보내고 되돌렸다
(`project.godot` 은 커밋하지 않는다).

```
godot --headless --path . --export-release "Web" build/web/index.html
python -m http.server 8731 --directory build/web
```

**브라우저에서 여는 것이 막혔다.** 미리보기 창이 `localhost:8731` 을 정책으로 거부한다
(다른 레인이 쓰는 `localhost:8231` 은 열린다). 포트를 8080 · 5173 · 3000 으로 바꿔도 같다.
Chrome 확장 쪽은 연결되어 있지 않다.

**남은 일은 브라우저에서 여는 것 하나다.**

| 준비된 것 | 상태 |
| --- | --- |
| 측정 장면 | `tools/web_dungeon_bench.tscn` — 네이티브에서 돌려 위 표를 냈다 |
| 웹 빌드 | `build/web/` 에 나온다. `.gitignore` 대상이다 |
| 읽는 법 | 결과를 **행마다 즉시** 콘솔과 화면에 낸다. 마지막에 몰아 내면 안 끝났을 때 앞의 것도 못 본다 |

**표본을 늘리지 마라.** 이동 레인이 웹에서 표본을 늘렸다가 wasm 메모리로 죽었다.
지금 120 판 • 두 조합은 그 사고를 피하려고 줄인 값이다.

---

## 17.22 게이트 목록이 첫 판에서 갈리지 않던 것 (2026-08-09)

### 17.22.1 증상과 원인

길드 등급 1 에서 게이트 넷이 **전부 같았다.** `GateRank.is_open_to` 가
`int(kind) < guild_rank` 라 등급 1 은 E급만 열 수 있고, `GateBoard.create` 의 범위가
`lowest = highest = 0` 으로 접혔기 때문이다.

**그리고 더 있었다 — `Gate.dungeon_character` 를 아무도 배정하지 않았다.**
§17.18 에서 성격을 배선했지만 게시판이 그것을 쓰지 않아 **넷이 전부 성격 0** 이었다.
등급도 성격도 같으니 네 게이트가 같은 판을 열었다.

### 17.22.2 고친 방법 — 등급이 아니라 성격과 크기로 벌린다

**등급 상한은 못 올린다.** 못 들어가는 게이트를 목록에 올리면 "고르는 재미" 가
"안 되는 것 구경" 이 된다(`GateRank` 주석). 그래서 같은 등급 안에서 벌렸다.

| 무엇 | 어떻게 |
| --- | --- |
| 성격 | 게시판이 다섯을 **겹치지 않게 섞어** 나눠 준다 (피셔-예이츠, 시드에서 나온다) |
| 크기 | `Gate.size_offset` (-1 / 0 / +1). 같은 등급 안에서 판 크기가 흔들린다 |

**크기를 흔드는 것이 위험 대 보상의 저울을 돌린다** — 넓은 판은 방이 많아 귀중품도 많고
그만큼 오래 머문다. 성격만 흔들면 「복잡한데 보상은 같다」가 되어 고를 이유가 없다.

### 17.22.3 결과 — 길드 등급 1 목록

| 목록 시드 1 | 등급 | 성격 | 규모 | 복잡 | 험난 |
| --- | --- | --- | --- | --- | --- |
| 울리는 저수조 | E급 | 금고층 | 1 | **3** | 2 |
| 검은 저수조 | E급 | 먼 출구 | 1 | 2 | 1 |
| 잿빛 터널 | E급 | 벌집 | **2** | **3** | 1 |
| 검은 계단실 | E급 | 얕은 갱도 | 1 | **1** | 2 |

**넷이 갈린다.** 테스트가 이것을 못 박는다 — 목록 시드 다섯 개에서
「성격·크기 조합이 셋 이상」과 「화면에 나오는 등급 조합이 셋 이상」을 함께 본다.
**성격이 달라도 등급이 같으면 플레이어에게는 같은 게이트다.**
