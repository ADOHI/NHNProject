# 기획 문서

> **상태: 초안.** 게임 방향이 확정되지 않았다.
> 아래 §1 은 확정된 제약이고, §2 부터는 결정 대기 항목이다.
> 결정될 때마다 이 문서를 갱신하고, 결정의 **근거**를 함께 남긴다.

---

## 1. 확정된 제약

기획 이전에 이미 고정된 조건이다. 기획은 이 안에서 이뤄져야 한다.

| 항목 | 값 | 비고 |
| --- | --- | --- |
| 엔진 | Godot 4.7.1 | GDScript |
| 제출 형태 | **웹(HTML5) 빌드** | 브라우저에서 즉시 실행 가능해야 함 |
| 배포 | GitHub Pages 자동 배포 | https://adohi.github.io/NHNProject/ |
| 렌더러 | `gl_compatibility` (WebGL 2) | Vulkan 불가 |
| 스레드 | 단일 스레드 | `SharedArrayBuffer` 사용 불가 |
| 기준 해상도 | 1280 × 720 | 창 크기에 맞춰 스케일 |
| 차원 | 2D | |

### 제약이 기획에 주는 영향

웹 빌드라는 조건은 단순한 배포 방식이 아니라 **기획 제약**이다.

- **초기 로딩이 곧 이탈 지점이다.** 브라우저에서 여는 순간 다운로드가 시작된다.
  에셋 용량이 커질수록 플레이 전에 이탈한다. 고해상도 텍스처와 긴 BGM 은 비용이 크다.
- **소리는 첫 입력 전까지 재생되지 않는다.** 브라우저 정책이다.
  타이틀 화면에서 바로 BGM 이 나오는 연출은 성립하지 않는다.
- **단일 스레드다.** 무거운 연산을 별도 스레드로 넘길 수 없다.
  한 프레임에서 오래 걸리는 처리는 화면 전체를 멈춘다.
- **입력 환경을 가정할 수 없다.** 채점자가 어떤 기기·화면으로 열지 모른다.
  키보드 전용 조작은 위험하고, UI 는 화면 비율 변화를 견뎌야 한다.

---

## 2. 게임 개요

**미정.** 아래 항목이 결정되어야 이후 설계가 진행된다.

- 장르
- 핵심 재미 (플레이어가 반복하게 되는 행동 한 가지)
- 1회 플레이 시간
- 승패 조건

## 3. 조작

**미정.** 결정 시 `project.godot` 의 InputMap 에 액션으로 정의한다
(키 코드를 코드에 직접 쓰지 않는다).

## 4. 게임 규칙

**미정.**

## 5. 콘텐츠 범위

**미정.** 제출 일정에 맞춘 최소 범위와 여유 시 추가할 범위를 나눠서 정한다.

## 6. 아트 · 사운드 방향

**미정.** 웹 로딩 시간 제약(§1)을 고려해 용량 상한을 먼저 정하는 편이 낫다.

---

## 7. 결정 기록

중요한 기획 결정을 시간순으로 남긴다. 번복된 결정도 지우지 않고 사유와 함께 남긴다.

| 날짜 | 결정 | 근거 |
| --- | --- | --- |
| 2026-08-07 | 제출 형태를 웹 빌드로 확정 | 제출 요건 |
| 2026-08-07 | 엔진을 Godot 4.7.1 로 확정 | 제출 요건 |
