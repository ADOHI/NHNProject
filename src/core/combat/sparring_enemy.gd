class_name SparringEnemy
extends RefCounted
## 대련장에 서 있는 적 하나. **자리와 무기뿐이다.**
##
## `docs/design/28-combat.md` §28.20.34.
##
## ## 눈금과 시계는 여기 없다
##
## 무너짐 눈금과 「다음 타가 언제인가」는 `SparringBout` 이 들고 있다.
##
## **수명이 다르기 때문이다.** 눈금은 판이 시작할 때 0 이 되고 판이 끝나면 뜻이 없다.
## 자리는 판보다 오래 산다 — 판을 다시 쏴도 적은 그 자리에 서 있다.
## 한 객체에 섞어 두면 다시 시작할 때 **무엇을 지우고 무엇을 남길지**가 매번 헷갈린다.

enum Kind {
	MONSTER,  ## 죽는다 — 전원을 눕혀야 끝난다 (§28.10)
	PERSON,  ## 제압된다 — 우두머리가 누우면 나머지가 물러난다 (§28.10)
}

## 대련장 좌표(픽셀). 화면 좌표와 같은 축을 쓴다 (오른쪽이 +).
var x: float = 0.0

## 이 적이 들고 있는 것. `null` 이면 반격하지 않는다.
var weapon: BackpackItem = null

## **몇 째 켜인가** (0 이 앞줄). `docs/design/28-combat.md` §28.20.38.
##
## ## 왜 좌표가 아니라 켜인가
##
## 지금까지 적은 **한 줄**로 섰다 — 저마다 다른 거리에. 그래서 「뒷줄」이라는 것이 없었고
## 걸침도 표적 고르기도 **전부 닿는 판**에서만 재였다.
##
## 좌표(가로세로)를 넣으면 **이동 · 충돌 · 자리 다툼이 딸려 온다.** 그건 §28.4 의 몫이다.
## 켜 번호만으로도 물을 것은 다 물을 수 있다 — 뒷줄에 닿나, 걸침이 앞뒤로도 걸리나,
## 못 닿는 놈을 고르면 어떻게 되나.
##
## **같은 켜에 선 적은 거리가 같다.** 가로 축이 아예 없는 셈이다.
var rank: int = 0

## **몬스터인가 사람인가** (§28.10 · §28.20.44).
##
## §28.10 이 확정했다 — *"몬스터는 죽고 사람은 제압이야."*
## 그러면 **싸움이 끝나는 조건이 적에 따라 갈린다** (§28.20.43 의 손잡이).
##
## **싸우는 방식은 이걸로 안 갈린다.** 여기 있는 것은 **판정**을 위한 표시다 —
## 몬스터는 전원을 눕혀야 끝나고 사람은 우두머리를 제압하면 나머지가 물러난다.
var kind: Kind = Kind.MONSTER

## **이 무리의 우두머리인가.** 사람 무리에만 뜻이 있다.
##
## 우두머리는 **특정한 하나**다. §28.20.43 의 `ANY_ONE`(아무나 하나)과 다르다 —
## 그래서 **아무 규칙도 우두머리를 노리지 않으면 훨씬 오래 걸린다** (§28.20.44).
var is_leader: bool = false


func _init(p_x: float = 0.0, p_weapon: BackpackItem = null, p_rank: int = 0) -> void:
	x = p_x
	weapon = p_weapon
	rank = p_rank
