# NHNProject 작업 지침

Godot 4.7.1 기반 2D 게임. **PC 스탠드얼론(Windows)으로 출시**한다.
**웹(HTML5) 빌드는 데모 배포·디버깅용으로 유지한다** — 등급이 내려갔을 뿐 버린 것이 아니다.

## 개발 원칙

1. **문서화가 먼저다.**
   무엇을 왜 만드는지 **먼저 적고** 시작한다. 만들면서 판단이 바뀌면 그때그때 문서를 고친다.
   기능이 끝난 뒤 문서를 몰아 쓰는 것은 금지다 — 같은 커밋에 들어가더라도 마찬가지다.

   **근거:** 2026-08-07 세션이 앱 중단으로 끊겼을 때, 살아남은 작업은 **문서를 미리 써 둔 것뿐**이었다.
   레인 하나는 대화 기록이 통째로 사라졌는데 `docs/` 에 조사 결과와 결론이 남아 있어 이어받을 수 있었다.
   문서를 마지막에 쓰기로 했다면 처음부터 다시였다.
   **문서는 결과물이 아니라 진행 중인 작업의 백업이다.**
2. **재사용성을 최대한 고려한다.**
   노드 트리에 의존하지 않는 로직은 `src/core/` 의 순수 클래스로 분리한다.
   특정 씬에서만 쓰이는 코드라도, 두 번째 사용처가 생길 여지가 보이면 미리 분리한다.
3. **폴더·파일 구조는 보편적 컨벤션을 따른다.** → `docs/conventions.md`
4. **리팩토링은 과감하게 하되, 기능 유지를 반드시 검증한다.**
   구조가 잘못됐다고 판단되면 미루지 말고 고친다.
   단 리팩토링 커밋은 반드시 실행 검증(아래)을 거친다. "고쳤는데 안 돌아간다"는 허용되지 않는다.
5. **개선안은 제안하고 허가받은 뒤 착수한다.**
   최신 기법, 미래에 필요할 구조, 완성도를 높일 요소는 **적극적으로 제안한다.**
   제안하지 않고 넘어가는 것도 위반이고, 승인 없이 임의로 넣는 것도 위반이다.

## 검증 규칙

코드 변경 후 아래를 통과해야 커밋한다. 코드 리뷰만으로 채택하지 않는다.

```bash
godot --headless --path . --import                # 임포트 오류 없음
godot --headless --path . --quit-after 60         # 런타임 오류 없음
python tools/run_gut_retry.py --expect-tests N    # 테스트 통과
python tools/run_lint_retry.py                    # 컨벤션 통과 (src test tools)
```

CI 는 `quality.yml` 에서 **날것으로** 검사한다 — `gdlint src test tools` ·
`gdformat --check src test tools` · `godot -s gut_cmdln.gd`. CI 러너는 멀쩡한 기계라
그래도 된다. **위 두 러너는 이 개발 기계 전용 우회다.**

> ### ⚠ 이 기계에서는 날것으로 돌리면 못 끝난다
>
> 무작위 `0xC0000005` 가 난다 (원인은 하드웨어 — 아래 GPU 절의 정정과
> [`docs/test-stability.md`](docs/test-stability.md)).
> **폭사율이 그 프로세스가 한 일의 양에 비례한다.**
>
> | 부하 | 폭사율 |
> | --- | --- |
> | `gdlint <파일 하나>` | 1 / 10 |
> | `gdlint src test tools` (약 530 파일) | **12 / 12 — 한 번도 못 끝냈다** |
> | GUT 전체 스위트 | 5 / 12 |
>
> 두 러너는 **긴 실행 하나를 짧은 실행 여럿으로 쪼개고, 폭사한 것만 다시 돌린다.**
> **단정 실패와 진짜 지적은 재시도하지 않는다** — 그것까지 재시도하면
> 검사가 아니라 로또가 된다. 판정을 못 얻은 파일이 남으면 **빨갛게 끝난다.**
>
> `--expect-tests` 로 **덜 돌고 통과한 실행**을 막아라. 이 저장소는
> GUT 의 `-gtest=` 가 조용히 무시되는 것에 이미 한 번 당했다.

**설명이 안 되는 실패를 만나면 코드를 파기 전에 한 번 더 돌려라.**
같은 결함이 폭사로도 나타나지만 **조용히 틀린 값**으로도 나타난다 —
정적 타입 매개변수의 타입 태그가 실행 중에 뒤집힌 것을 관측했다.
**결정적 입력에 결정적 코드인데 결과가 갈리면 코드가 아니다.**

구조를 옮기는 변경(파일 이동/이름 변경)은 `.godot/` 캐시를 지우고 검증한다.
캐시가 깨진 경로를 가려 준다.

웹 빌드에 영향이 갈 수 있는 변경은 푸시 후 GitHub Actions 결과와 배포본까지 확인한다.

## 병렬 작업

여러 파일에 걸치는 긴 작업은 **하위 에이전트에게 백그라운드로 넘긴다.**
이유는 속도가 아니라 두 가지다.

- **대화가 살아 있다.** 한 턴을 도는 동안에는 사용자 메시지를 읽을 방법이 없다.
  직접 오래 돌면 그 시간 내내 응답이 끊긴다. 넘기면 즉시 대화권이 돌아오고,
  돌아가는 중에 받은 지시를 해당 에이전트에게 전달할 수 있다.
- **겹치지 않는 작업은 동시에 끝난다.**

방향이 틀어진 것이 보이면 사용자는 기다리지 말고 **중단**(터미널이면 `Esc`)한다.

### 레인은 명령 단위로 산다

**지시받은 작업에 대해서만 레인을 띄우고, 끝나면 죽인다.**
앞으로 필요할 것 같다는 이유로 여러 레인을 미리 깔지 않는다.

오래 살아 있는 레인은 통제를 벗어난다. 사용자가 방향을 바꿔도 레인은
이미 옛 지시대로 만들고 있고, 그 결과물은 버려진다.
지시 하나 = 레인 하나 = 결과 하나로 묶여 있어야 무엇을 받을지 예측할 수 있다.

버려진 레인은 워크트리와 브랜치까지 지운다. 남겨 두면 다음 통합 때
어느 것이 살아 있는 작업인지 알 수 없게 된다.

```bash
git worktree remove --force <경로> && git branch -D <브랜치> && git worktree prune
```

### GPU 를 나눠 쓴다 — 레인이 여럿일 때 가장 자주 깨지는 곳

2026-08-07 세션이 앱 중단으로 끊겼다. 원인을 파 보니 **특정 앱의 버그가 아니었다.**

| 시각 | 죽은 것 | 죽은 위치 |
| --- | --- | --- |
| 15:08 | `python.exe` | `python310.dll` |
| **15:18 · 15:23** | **Godot** | **`nvoglv64.dll` — 두 번 다 같은 오프셋** |
| 15:30 | `powershell.exe` | `clr.dll` |
| 16:14 | `conhost.exe` | 멈춤 → 강제 종료. **세션이 여기서 끊겼다** |

서로 무관한 프로세스가 서로 다른 모듈에서 전부 `0xc0000005` 로 죽었다.
그때 **VRAM 은 16.4GB 중 9.8GB 가 물려 있었고** ComfyUI 가 모델을 올린 채였다.
OS 는 재부팅되지 않았다 — 앱만 죽었다.

> ### ⚠ 2026-08-18 정정 — **VRAM 은 원인이 아니었다. 상관을 인과로 읽었다**
>
> 같은 증상을 **VRAM 이 깨끗한 상태에서** 다시 만났다 (여유 15.5GB / 16.4GB, ComfyUI 안 떠 있음).
>
> - Godot 뿐 아니라 **`gdformat`(Python)** 도 같은 코드로 죽는다 — 같은 파일 12회 중 2회
> - **셸과 무관하다** — Git Bash 2/12, PowerShell 2/12
> - **CPU 가 직접 보고했다** — `WHEA-Logger` 19: *Corrected Machine Check,
>   **Translation Lookaside Buffer Error**, reported by **Processor Core***
> - **최근 3주 커널 버그체크 5회** — `0x1E`·`0x50`×3·`0x3B`, 전부 메모리 오염 계열
> - 기계는 **13th Gen Intel Core i9-13900H**, RAM 은 정격(오버클럭 아님)
>
> **원인은 기계다** (증거·처방: [`docs/test-stability.md`](docs/test-stability.md)).
> 위 표의 2026-08-07 도 같은 사건이었을 가능성이 높다.
>
> **아래 규칙 셋은 그대로 지킨다** — 원인이 아니어도 셋 다 그 자체로 옳다.
> 바뀌는 것은 하나다: **테스트가 무작위로 죽어도 먼저 코드를 의심하지 마라.**
> 실패가 `0xc0000005`/신호 11 이고 단정 실패가 없으면 **기계를 먼저 의심하고 다시 돌려라.**

**규칙 셋.**

1. **이미지 생성 모델은 하나만 올린다.** 인스턴스를 새로 띄우지 말고 체크포인트도 갈아 끼우지 마라.
   예전에 로컬 ComfyUI 를 둘 띄웠다가 블루스크린이 났다
2. **레인이 여럿이면 순번을 물린다.** 통합자가 사용권을 지정하고, 쓰는 쪽은 **끝나는 즉시 알린 뒤
   `clear_vram` 으로 놓는다.** 붙잡고 있으면 다음 레인이 못 쓴다.
   기다리는 레인은 그동안 이미지가 필요 없는 일(조사 · 씬 뼈대 · 셰이더 · 모션)을 먼저 한다
3. **창을 띄우는 Godot 실행을 아낀다.** 우리는 `gl_compatibility`(OpenGL)라 캡처마다
   GL 컨텍스트를 새로 만든다. 테스트 · 임포트 · 린트 · 글리프는 `--headless` 로 충분하고,
   **창 캡처는 한 번 띄울 때 여러 장을 몰아서** 뽑는다. 띄우고 닫기를 반복하지 마라

### 워크트리는 나중 커밋을 못 본다 — 레인은 낡은 기획서를 읽는다

레인은 **띄운 시점의 커밋에서 갈라져 나온다.** 그 뒤에 통합자가 기획 문서를 고쳐도
**레인의 파일은 그대로다.** 레인은 자기가 최신을 읽고 있다고 믿는다.

**실제로 사고가 났다.** 세계관을 게이트물로 교체한 뒤에도 타이틀 레인은 폐기된
"방송용 시설" 절을 읽고 조명 트러스와 관객석을 그림에 넣었다. 두 번 났다 —
한 번은 폐기본을 현행으로 읽어서, 한 번은 워크트리가 낡아서.

**통합자가 할 일 둘.**

1. **공용 문서를 고치면 돌고 있는 레인에 내용을 직접 보낸다.** 문서를 고쳤다고 전달된 것이 아니다
2. **그리고 `git merge main` 을 시켜라.** 레인은 자기 디렉터리만 건드리므로 대개 깨끗하게 붙는다.
   내용만 보내고 파일을 안 고치면, 그 레인이 나중에 같은 절을 다시 읽는다

**레인이 할 일.** 기획 문서를 인용하기 전에 `git log -1 --format=%h main` 과 자기 `HEAD` 를
비교해 보고, 뒤처져 있으면 통합자에게 알려라.

### 나누는 기준 — 파일이 겹치지 않을 것

| 작업 성격 | 처리 |
| --- | --- |
| 새 디렉터리를 만든다 | 병렬 가능. 각자 독립 워크트리에 격리한다 |
| 기존 파일을 고친다 | 직렬. 같은 파일을 두 레인이 만지면 덮어쓴다 |
| 인터페이스를 공유한다 | 인터페이스를 먼저 확정한 뒤 병렬 |

### 브리핑에 반드시 넣을 것

에이전트는 **빈 문맥으로 시작한다.** 이 저장소가 쌓아 온 결정을 하나도 모르고,
막혀도 사용자에게 물어볼 수 없어 애매하면 그냥 추측한다. 그래서 브리핑이 유일한 통제 수단이다.

1. **문서를 먼저 쓰고 시작하라** (개발 원칙 1). 어디에 쓸지 파일명까지 지정한다.
   **레인은 언제든 끊길 수 있고, 그때 남는 것은 디스크에 쓴 것뿐이다**
1. **덩어리마다 커밋하라.** 검증이 다 끝나야 커밋하는 것이 아니다 —
   덜 된 것은 `wip:` 로 박아 두고 다음에 다듬는다.
   **2026-08-18 에 기계가 버그체크(`0xF7`)로 재부팅되면서 한 레인의 1,100 줄이
   커밋 안 된 채로 걸려 있었다.** 워크트리가 디스크에 남아 살렸지만, 살린 것은
   운이다. 이 기계는 3주에 여섯 번 죽었다 — **다음 재부팅은 예고 없이 온다**
2. 읽어야 할 기획 문서와 **절 번호까지**
3. 개발 원칙 2 — `src/core/` 는 노드 트리에 의존하지 않는 `RefCounted`
4. 커밋 전 위 **검증 규칙 4줄 + 글리프 검사** 통과
5. **건드리면 안 되는 파일** — 아래 공용 문서 3개, 다른 레인의 디렉터리
6. 테스트 동반. 단일 파일에 몰아넣지 않는다
7. 판단이 갈릴 지점은 **미리 정해서 준다.** 물어볼 수 없으므로 비워 두면 추측이 들어온다

### 통합은 넘기지 않는다

- 결과를 **그대로 합치지 않는다.** 헤드리스 테스트를 통과하고도 화면에서 파싱 에러로 터진 전례가 있다
- 전체 테스트와 렌더링 확인은 **통합 후 한 번에** 돌린다
- 아래 문서는 모든 레인이 건드릴 대상이라 **한 명이 쓴다**

| 공용 문서 | 이유 |
| --- | --- |
| `docs/design/11-decisions.md` | 결정 기록이 한 줄로 이어져야 한다 |
| `docs/ai-usage.md` | 제출물. 작업 로그와 프롬프트 순번이 어긋나면 안 된다 |
| `docs/architecture.md` | 전체 구조를 한눈에 보이게 유지해야 한다 |

## 웹을 깨지 않는다 — 본판이 PC 로 바뀐 뒤에도

**2026-08-19 에 본판이 PC 스탠드얼론이 됐다.** 웹은 **데모·디버깅용으로 남는다.**
그러므로 아래 설정은 **여전히 지킨다** — 다만 이유가 「제출 조건」에서 「데모를 살려 둔다」로 바뀌었다.
근거는 `docs/web-build.md` 에 있다.

| 설정 | 값 | 왜 |
| --- | --- | --- |
| `rendering/renderer/rendering_method` | `gl_compatibility` | 브라우저에 Vulkan 이 없다 |
| `variant/thread_support` (Web 프리셋) | `false` | GitHub Pages 는 COOP/COEP 헤더를 못 준다 |

> ### PC 만 Vulkan 으로 올릴 수 있다 — **아직 안 켰다**
>
> Godot 은 플랫폼마다 렌더러를 따로 준다 (`rendering_method.web` 로 웹만 GL 로 둘 수 있다).
> **그런데 안 켰다.** 셰이더 전부가 `gl_compatibility` 에서만 판정받았고,
> 렌더러를 가르면 **화면을 두 벌 검수해야 한다.**
> 켜려면 먼저 재고 무엇이 좋아지는지 보여라 → `docs/design/01-constraints.md` §1.1.1

### PC 가 본판이 되면서 바뀐 것 — **「아끼지 마라」**

용량 예산이 사실상 없어졌다. 웹 용량을 걱정해서 UI 를 도형과 셰이더로 몰아넣었다가
한 번 갈아엎은 전례가 있다. **그 걱정의 근거가 이제 없다** —
소리 · 초상 · 문안 · 그림을 용량 때문에 줄이지 마라.

**다만 무거운 루프를 한 프레임에 몰지 않는 규율은 그대로다.** 웹 데모가 같은 코드로 돌고,
쪼개진 코드는 안 쪼개진 코드보다 나쁠 것이 없다.

## AI 활용 기록

`docs/ai-usage.md` 는 제출물이다. 작업 세션이 끝날 때마다
§4 작업 로그와 §5 프롬프트 전문을 갱신한다. 프롬프트는 원문 그대로 남긴다.

PDF 변환:

```bash
python tools/build_docs.py
```

## 문서 지도

| 문서 | 내용 |
| --- | --- |
| `docs/game-design.md` | 기획 문서 지도 — 어떤 내용이 어느 파일에 있는가 |
| `docs/design/*.md` | 기획 본문 — 무엇을 만드는가 (항목별 분할) |
| `docs/conventions.md` | 개발 컨벤션 — 어떻게 쓰는가 |
| `docs/architecture.md` | 구조 — 지금 무엇이 있는가 |
| `docs/web-build.md` | 웹 빌드·배포 |
| `docs/ai-usage.md` | AI 활용 기술 문서 (제출물) |
